# TASKS.md — SafetyWalk / 현장안전 지킴이

## Status Legend
- [ ] Not started
- [~] In progress
- [x] Done

## Priority Legend
- **P0** Blocking MVP
- **P1** Should ship with MVP
- **P2** Defer if needed
- **Risk** Handle carefully — technical or product risk

---

## Phase 0: Project Foundation

| # | Task | Priority | Notes |
|---|---|---|---|
| 0-1 | [x] Create Xcode project: SwiftUI, iOS 17+, SwiftData | P0 | Existing project found; App.swift template placeholder fixed; SafetyWalkApp struct wired |
| 0-2 | [x] Define folder structure: Models/, Views/, ViewModels/, Services/, Resources/Localization/, Resources/Templates/ | P0 | All directories and placeholder files created |
| 0-3 | [x] Add Localizable.strings for EN and KO | P0 | en.lproj and ko.lproj created; all P0 strings present — must add to Xcode project (see Xcode steps below) |
| 0-4 | [x] Define LocalizationKey enum for type-safe string access | P0 | Utilities/LocalizationKey.swift |
| 0-5 | [x] Define all Swift enums from DOMAIN_TERMS.md | P0 | Models/Enums.swift |
| 0-6 | [x] Define all SwiftData @Model classes | P0 | Models/Site.swift, Area.swift, Inspection.swift, ChecklistItem.swift, Hazard.swift |
| 0-7 | [x] Define RegionProfile model and settings storage | P0 | Utilities/RegionProfileStore.swift — static get/set, stores "KR"/"GLOBAL", defaults to .korea |
| 0-8 | [x] Add camera and photo library usage descriptions to Info.plist | P0 | Confirmed in project.pbxproj: INFOPLIST_KEY_NSCameraUsageDescription + NSPhotoLibraryUsageDescription, both Debug and Release |

---

## Phase 1: Data Layer

| # | Task | Priority | Notes |
|---|---|---|---|
| 1-1 | [x] Implement SwiftData model container setup | P0 | .modelContainer(for: [Site, Area, Inspection, ChecklistItem, Hazard]) on WindowGroup |
| 1-2 | [x] Implement PhotoStorageService: save photo to Documents, return path | P0 | UUID filename; JPEG 0.82; 1024px max; injectable rootDirectory for tests |
| 1-3 | [x] Implement ChecklistTemplateLoader: load JSON from bundle by region | P0 | Services/ChecklistTemplateLoader.swift — load(for:) throws, injectable bundle, two error cases |
| 1-4 | [x] Write Korea region checklist template JSON | P0 | checklist_korea.json — 3 categories, 11 items; all titleKeys in EN+KO .strings |
| 1-5 | [x] Write Global region checklist template JSON | P0 | checklist_global.json — 4 categories, 12 items; all titleKeys in EN+KO .strings |
| 1-6 | [x] Write unit tests for ChecklistTemplateLoader | P0 | 24 tests across 4 suites; nonisolated fix in production code for MainActor isolation |
| 1-7 | [x] Write unit tests for PhotoStorageService | P1 | 15 tests across 4 suites; scale=1.0 fix in production resized() |

---

## Phase 2: Core Screens — P0

| # | Task | Priority | Notes |
|---|---|---|---|
| 2-1 | [x] HomeView: empty state, recent inspections list, Start Inspection button | P0 | HomeViewModel: date/status/counts; @Query inspections+hazards; Start Inspection button is no-op until 2-2 |
| 2-2 | [x] StartInspectionFlow: site selection step | P0 | List of saved sites + inline Add New Site form |
| 2-3 | [x] StartInspectionFlow: area selection step | P0 | Saved areas + free text + Skip option |
| 2-4 | [x] StartInspectionFlow: template selection step | P0 | Loads Korea+Global; pre-selects by RegionProfileStore |
| 2-5 | [x] ChecklistView: scrolling list of checklist items with Pass/Fail/NA buttons | P0 | Progress header; grouped by category; Complete button disabled until all checked; @Query on ChecklistItem |
| 2-6 | [x] ChecklistItemRow: result buttons, photo attach, note field | P0 | 44pt result buttons; note TextField shown after result selected; camera button placeholder (disabled); tapping active result deselects it |
| 2-7 | [x] CameraService: wrap AVFoundation or use PHPickerViewController | P0 | isCameraAvailable; checkPermission(); requestPermission() async; all nonisolated; no UI, no saving |
| 2-8 | [x] Inline photo attach in ChecklistItemRow | P0 | PhotosPicker; saves via PhotoStorageService; thumbnail shown; replace supported; live camera for later |
| 2-9 | [x] "Add Hazard" prompt on Fail result | P0 | Button shown on Fail+unlinked; "Hazard linked" indicator when linked; sheet presents HazardRegistrationView with inspection+item |
| 2-10 | [x] HazardRegistrationView: full form with camera, type, risk, description, status | P0 | PhotosPicker; PhotoStorageService on save; risk color buttons; all fields validated; links ChecklistItem.linkedHazardId |
| 2-11 | [x] InspectionSummaryView: completion screen with counts | P0 | Header card; Pass/Fail/NA counts; hazards by risk; failed items list; Share placeholder (disabled); cascade dismiss to Home |
| 2-12 | [x] InspectionDetailView: read-only view of completed inspection | P0 | Header card; progress counts; Continue button for in-progress; grouped items with result icons + photo thumbnails; linked hazards; HomeView nav updated |
| 2-M1 | [x] Create project skills for navigation QA, checklist expansion, and screen review | P1 | .claude/skills/navigation-qa/, .claude/skills/checklist-template-expansion/, .claude/skills/screen-implementation-review/ |
| 2-M2 | [x] Keep CLAUDE.md concise and move detailed procedures into skills | P1 | 63 lines; disclaimer, model summary, arch rules, testing notes moved to skills |
| 2-M3 | [x] Expand Korea and Global checklist templates to broad industrial safety coverage | P1 | 14 categories × 3 items = 42 items each; all keys in en+ko .strings; plutil OK; cross-check OK |
| 2-M4 | [x] Refactor ChecklistView into category-first checklist flow | P1 | ChecklistView shows 14 CategoryRow cards; ChecklistCategoryDetailView shows per-category items; ChecklistItemRow promoted to internal |
| 2-M5 | [x] Add SwiftUI build QA skill for repeated type inference issues | P1 | .claude/skills/swiftui-build-qa/SKILL.md; screen-implementation-review updated to reference it |
| 2-M6 | [x] Fix ChecklistView complete freeze (scroll + tap + back button) | P0 | Root cause: @Query inside NavigationLink destination in ForEach; 14 eager @Query inits stalled main thread. Fix: removed @Query from ChecklistCategoryDetailView; parent ChecklistView passes group.items as let array |
| 2-M7 | [x] Add SwiftData + NavigationLink @Query freeze lesson to skills | P1 | navigation-qa: new rule section + Bad/Good code example + diagnostic procedure; screen-implementation-review: SwiftData Safety check updated; TASKS.md row added |
| 2-13 | [x] HazardsTabView: list with filter by risk level and status | P0 | @Query in parent only; in-memory filter (RiskLevel + CorrectiveActionStatus); filter chips via Menu; HazardRowView shows thumbnail, description, location, risk badge, status badge; empty state + filtered empty state |
| 2-14 | [x] HazardDetailView: full view + editable corrective action status | P0 | @Bindable var hazard; no @Query; photo, info, description, segmented status picker with .onChange → modelContext.save() + updatedAt stamp; timestamps section |
| 2-15 | [x] HistoryTabView: list of past inspections | P0 | @Query in parent; NavigationLink(value:) + .navigationDestination(for: Inspection.self) (lazy, no @Query in destination); segmented status filter (All/In Progress/Completed); InspectionHistoryRowView shows site, area, dates, progress, status badge; HomeViewModel reused for formatters |

---

## Phase 3: Settings & Configuration

| # | Task | Priority | Notes |
|---|---|---|---|
| 3-M1 | [x] SettingsViewModel: inspectorName + regionProfile (UserDefaults/RegionProfileStore) + appVersion | P0 | ViewModels/SettingsViewModel.swift; no SwiftUI/SwiftData; UserDefaults key matches StartInspectionViewModel |
| 3-1 | [x] SettingsView: inspector name field, region profile segmented picker, site mgmt placeholder, DisclaimerView, app version | P1 | Views/Settings/SettingsView.swift; @State + @Bindable vm; no new strings; plutil OK |
| 3-M2 | [x] InspectionView: active inspections hub; @Query all→filter .inProgress; NavigationLink(value:)+navigationDestination; Start Inspection sheet; empty state | P1 | Views/Inspection/InspectionView.swift; added inspection.noActiveInspections; plutil OK |
| 3-M3 | [x] Fix regression: InspectionView .safeAreaInset was on NavigationStack (overlaid all pushed views); moved inside each root content branch | P0 | InspectionView.swift: .safeAreaInset moved onto emptyState and List separately |
| 3-M4 | [x] Fix regression: InspectionDetailView continueButton used closure NavigationLink; when status→.completed button disappeared and popped ChecklistView mid-summary push (freeze). Replaced with Button + @State showChecklist + .navigationDestination(isPresented:) | P0 | InspectionDetailView.swift |
| 3-M5 | [x] Fix freeze: ChecklistView used closure NavigationLink for 14 category rows; destinations (ChecklistCategoryDetailView with 3×ChecklistItemRow @State) eagerly constructed on push → main thread stall. Replaced with NavigationLink(value: CategoryRoute) + .navigationDestination(for: CategoryRoute.self) | P0 | ChecklistView.swift |
| 3-M6 | [x] Fix completion stability: InspectionView registered .navigationDestination(for: Inspection.self) only inside else branch; when inspection completes activeInspections empties, branch switches, destination unregisters while InspectionDetailView is on stack. Wrapped branches in Group and moved .navigationDestination to Group level | P0 | InspectionView.swift |
| 3-M7 | [x] Fix Continue Inspection freeze (BLOCKER): `@Query(filter: #Predicate)` inside a destination pushed at depth-2+ via `.navigationDestination(isPresented:)` froze the entire app on iOS 17/18 — destination `.onAppear` never fired; same `@Query` safe at depth 0/1. Isolated through 7+2 staged-rebuild iterations (InspectionDetailView S1–S7 + ChecklistDiagStage C1/C2/C2b). Fix: ChecklistView and InspectionSummaryView read items via `inspection.items.sorted { $0.sortOrder < $1.sortOrder }` (relationship-backed). @Query and init removed from both | P0 | ChecklistView.swift, InspectionSummaryView.swift; navigation-qa skill updated with the depth-2+ @Query rule |
| 3-M8 | [x] Fix category-detail back navigation: ChecklistView at depth-2 stacked both `.navigationDestination(for: CategoryRoute.self)` (categories) and `.navigationDestination(isPresented: $showSummary)` (summary). Back from ChecklistCategoryDetailView landed on the wrong screen. Fix: removed CategoryRoute + `.navigationDestination(for:)`; switched category rows to closure-based `NavigationLink { ChecklistCategoryDetailView(...) }` — safe because the destination owns no @Query | P0 | ChecklistView.swift; navigation-qa skill updated with the co-located-destination rule |
| 3-M9 | [x] Capture SwiftUI NavigationStack + SwiftData freeze lessons in Skills | P1 | navigation-qa: added Rule B (destination weight beyond @Query) with decision matrix, expanded diagnostic procedure into a 4-step staged-rebuild pipeline with console-pattern table, recorded the decisive 7-stage diagnostic sequence from 3-M7. screen-implementation-review: trigger now lists NavigationStack/NavigationLink/`.navigationDestination`/@Query as required `/navigation-qa` follow-up; added relationship-first preference and the depth-2+ / co-located checklist items. swiftui-build-qa: added a Scope note routing runtime freezes to /navigation-qa, plus a Related section |
| 3-M10 | [x] Add inspection scope/category selection before inspection creation | P1 | New step InspectionScopeSelectionView between Template and inspection creation. StartInspectionViewModel: `selectedCategoryKeys: Set<String>`, `selectAllCategories()`, `clearCategories()`, `applyRecommendedCategoryDefaults()`, `toggleCategory(_:)`, `isCategorySelected(_:)`, `generatedItemCount`, `canProceedFromTemplate`. `canBeginInspection` now also requires `!selectedCategoryKeys.isEmpty`. `createInspection(context:)` skips categories not in `selectedCategoryKeys` — unselected categories are EXCLUDED (no ChecklistItem created), NOT N/A. Recommended defaults: commonSafety, housekeeping, ppe, fire, emergencyResponse (intersected with active template, missing keys safely ignored). StartInspectionFlow.FlowStep gained `.scope`. TemplateSelectionView button changed from Begin (createInspection) to Next (push .scope). Old inspections unaffected (createInspection unchanged shape; existing items already persisted). 8 new localization keys added EN+KO; plutil OK |
| 3-M11 | [x] Fix scope template-change state and inactive Manage Sites row | P1 | StartInspectionViewModel: `selectedTemplate` now has `didSet` that clears `selectedCategoryKeys` when the new template id differs from the old — prevents stale keys from a previous template surviving a back/forward template change. Added `effectiveSelectedCount` (`selectedCategoryKeys.intersection(currentTemplateKeys).count`) used as defense-in-depth by `canBeginInspection` and by the scope-screen summary footer + warning-color trigger. SettingsView Manage Sites row: now uses HStack with a "Soon" / "준비중" capsule tag and `.disabled(true)` for system-applied disabled appearance — no longer looks tappable. Added `common.comingSoon` key in EN ("Soon") and KO ("준비중"); plutil OK |
| 3-2 | [x] SiteManagementView: create/edit/delete sites and areas | P1 | Views/Settings/SiteManagementView.swift. Root (depth-1 from Settings tab) owns @Query for Site, Inspection, Hazard — computes `referencedSiteIds` and `referencedAreaIds`. Pushes SiteDetailManagementView (depth-2) which owns NO @Query (per /navigation-qa Rule A) — receives @Bindable site + the two referenced sets. Site create: inline form (name + optional address). Site rename + address edit: in-place TextFields bound to @Bindable site. Site delete: blocked-alert if referenced (any Inspection.siteId or Hazard.siteId == site.id); confirm-alert otherwise. Area rename: in-place TextField via AreaEditRow with @Bindable area. Area add: inline form, creates Area + appends to site.areas. Area delete: blocked if any Inspection.areaId == area.id; confirm-alert otherwise. Existing inspections keep their denormalized siteName/areaName values — neither rename rewrites past records (history-preserving by design). SettingsView managementSection switched from disabled placeholder to active NavigationLink; "Soon" badge removed. 12 new localization keys added EN+KO (site.* and area.*); plutil OK |
| 3-3 | [ ] Language switch: app re-renders on language change | P2 (deferred) | Reclassified P1 → P2 at Phase 3 close. MVP path uses iOS Settings → app language; an in-app toggle is post-MVP. Not blocking Phase 3 close |
| 3-4 | [x] DisclaimerView: non-removable inline disclaimer; icon + title + body; reusable | P0 | Views/Settings/DisclaimerView.swift; added disclaimer.title to LocalizationKey + both .strings; plutil OK |
| 3-CLOSE | [x] **Phase 3 closed** | — | Final QA audit completed: 0 blockers, 0 highs, 1 medium (HazardRegistrationView sheet from depth-3 row) cleared via manual simulator verification — Fail item → Add Hazard sheet opens → save → dismiss → "Hazard linked" indicator → Back returns to ChecklistView overview, all navigation stable. 5 lows deferred to Phase 5 polish (see row 5-7). 3-3 reclassified P2 (deferred). All P0 + remaining P1 Phase 3 rows are complete |

---

## Phase 4: Share / Export

| # | Task | Priority | Notes |
|---|---|---|---|
| 4-1 | [x] Implement simple inspection summary renderer (SwiftUI View → UIImage) | P1 | Foundation PR landed: Views/Export/InspectionReportView.swift (pure-content SwiftUI view; no @Query, no .onAppear, no async, no nav). PhotoStorageService.loadDownsampled(relativePath:maxDimension:) uses CGImageSourceCreateThumbnailAtIndex for memory-efficient preview decode (kCGImageSourceThumbnailMaxPixelSize + WithTransform + ShouldCacheImmediately). Services/InspectionExportService.swift (@MainActor): renderImage(inspection:) async → UIImage? — preloads item + hazard photos off-main as plain UUID/UIImage dictionaries (no SwiftData objects cross actor boundary) then uses SwiftUI ImageRenderer at 612pt width, scale=2.0. Added 1 localization key (report.title EN+KO); plutil OK. PDF / ShareSheet wiring remains for 4-2 / 4-3 |
| 4-2 | [x] PDF export from rendered summary | P1 | Services/InspectionExportService.swift gained `exportPDF(inspection:) async -> URL?`. Refactored shared `makeRenderer(for:) async` private method — both `renderImage` and `exportPDF` consume it, so photo-preload + view-construction lives in one place. PDF path: `ImageRenderer.render { size, drawer in CGDataConsumer(url:) → CGContext(consumer:mediaBox:nil) → beginPDFPage(nil) → drawer(ctx) → endPDFPage → closePDF }`. Single-page tall PDF (no A4 pagination — explicit MVP scope per Phase 4 plan). Writes to `FileManager.default.temporaryDirectory.appendingPathComponent("SafetyWalk-<inspection.id>.pdf")`; overwrites prior export for the same inspection via `try? removeItem`. Returns nil on `CGDataConsumer`/`CGContext` failure. No UI wiring (deferred to 4-3); no new locale keys |
| 4-3 | [x] iOS share sheet integration (UIActivityViewController) | P1 | Views/Export/ShareSheet.swift: `UIViewControllerRepresentable` wrapping `UIActivityViewController` + `ShareItem: Identifiable` for `.sheet(item:)` lifecycle. Views/Export/ShareReportButton.swift: reusable button — owns `isExporting` + `shareItem` + `showFailedAlert` state; on tap kicks `Task { await InspectionExportService().exportPDF(inspection:) }`; spinner + "Preparing report…" while exporting; presents `ShareSheet` on success, localized alert on nil. Wired into InspectionSummaryView (replaces disabled placeholder) and InspectionDetailView (shown only when `inspection.status == .completed`, alongside the existing in-progress-only `continueButton`). Same export service pipeline at both entry points — no duplication. 3 new localization keys added EN+KO (`share.button`, `share.exporting`, `share.failed`); plutil OK. Sheet attached to depth-3 SummaryView — same accepted pattern as HazardRegistrationView; manual verification required |
| 4-4 | [x] Photo compression for export (max 800px per photo in summary) | P1 | Satisfied by 4-1b + shared 4-2/4-3 pipeline — no separate work needed. `PhotoStorageService.loadDownsampled(relativePath:maxDimension:)` uses `CGImageSourceCreateThumbnailAtIndex` with `kCGImageSourceThumbnailMaxPixelSize` (true ImageIO downsample — full-size image never decoded). `InspectionExportService.photoMaxDimension = 600` (more conservative than the original 800 spec — comfortable for KakaoTalk / Mail attachment sizes). Photos preloaded off-main via `Task.detached(priority: .userInitiated)`; `InspectionReportView` consumes preloaded `[UUID: UIImage]` dictionaries with no in-body I/O; PDF and image outputs share `makeRenderer(for:)`. Missing photo files return nil → report omits the image (no crash). ShareSheet ships only the file URL — no raw image data on the activity-controller path. Swift 6 actor isolation clean after the `nonisolated init` fix |

---

## Phase 5: Polish & MVP Readiness

| # | Task | Priority | Notes |
|---|---|---|---|
| 5-1 | [x] Onboarding: first-launch inspector name + region profile setup | P2 | New `Views/Onboarding/OnboardingView.swift`: title + subtitle + inspector-name TextField + region segmented Picker + `DisclaimerView` + Start button (disabled until trimmed name non-empty; orange caption shown otherwise). Uses `@AppStorage("com.safetywalk.hasCompletedOnboarding")` for the routing flag; persists name to `com.safetywalk.inspectorName` (matches SettingsViewModel key) and region via `RegionProfileStore.set(...)`. ContentView gated: `if hasCompletedOnboarding { mainTabView } else { OnboardingView() }` — flag flip causes automatic swap with no extra navigation. **NOT a login screen**: no account, no password, no email, no signup, no server, no roles — explicit comment in the view documents this. SettingsView remains the canonical editor for both values. 6 new localization keys (`onboarding.title/subtitle/inspectorName/region/start/nameRequired`) EN+KO; plutil OK |
| 5-2 | [x] Empty states for all list views | P0 | Audit-only — no code changes. Verified 14 screens: Home / Inspection / SiteSelection / AreaSelection / Template / Scope / Checklist / ChecklistCategoryDetail / InspectionDetail / InspectionSummary / Hazards / History / Settings / SiteManagement (and the depth-2 SiteDetail) all have appropriate empty / filtered-empty handling. Localization keys already in place: `home.noInspectionsYet`, `inspection.noActiveInspections`, `inspection.noSitesYet`, `inspection.noScopeSelected`, `hazard.noHazardsYet`, `hazard.noFilterResults`, `history.noRecords`, `history.noFilterResults`, `site.noSites`, `area.noAreas`. AreaSelectionView always shows "No specific area" as the first row so the screen is never visually empty. ChecklistView / ChecklistCategoryDetailView are gated upstream by the scope-required Begin button (zero-items state structurally impossible). TemplateSelectionView relies on bundled JSON; failure is a build-env issue, not a user-facing empty state |
| 5-3 | [x] Error handling: photo/storage failures surfaced as alerts | P1 | `ChecklistItemRow` (in ChecklistView.swift): refactored `saveSelectedPhoto` to surface load + save errors via two `.alert` modifiers + `showPhotoLoadFailed` / `showPhotoSaveFailed` state. Reordered to **save new photo first, then delete old** — `item.photoPath` is only updated after a successful new save, so a failed replacement preserves the existing photo. `HazardRegistrationView`: `loadSelectedPhoto` surfaces load errors; `save()` surfaces save errors (sheet stays open on failure; no Hazard is inserted; user can retry). Photo-required / location-required / description-required validation unchanged. 2 new localization keys: `error.photoLoadFailed`, `error.photoSaveFailed` (EN+KO); plutil OK. No live-camera UI added (out of MVP scope) — `CameraService` unchanged. Existing `share.failed` alert for export failure is untouched |
| 5-4 | [x] Accessibility: minimum 44pt tap targets, Dynamic Type support | P1 | Audit found 6 small gaps; all fixed. (1) ChecklistItemRow photoRow PhotosPicker: added `.controlSize(.large)` so the `.bordered` button reaches ≥44pt at default Dynamic Type. (2) Same for the Add Hazard button. (3) Pass/Fail/N/A result buttons: added `.accessibilityAddTraits(isSelected ? .isSelected : [])` so VoiceOver speaks the selection state (previously color-only). (4) HazardRegistrationView risk buttons: same `.accessibilityAddTraits` pattern. (5) ChecklistItemRow + InspectionDetailView.DetailItemRow thumbnail Images: added `.accessibilityHidden(true)` (decorative — the row text already conveys context). (6) SiteRowLabel mappin capsule: added `.accessibilityElement(children: .ignore)` + `.accessibilityLabel(String(format: site.areaCountAccessibility, count))` so VoiceOver says "3 areas" / "구역 3개" instead of "mappin 3". Added 1 localization key `site.areaCountAccessibility` (EN "%d areas" / KO "구역 %d개"); plutil OK. No legal/compliance wording introduced. Dynamic Type — all body fonts already use semantic styles (`.body`, `.subheadline`, `.caption`); minHeight constraints (not fixed heights) on primary buttons so they scale without clipping |
| 5-5 | [x] App icon and launch screen | P2 | **2026-05-17 updated (third revision)**: `AppIcon-1024.png` replaced with `image.png` (found at project root) — white/silver 3D photo-realistic shield with 2×2 grid, curved route, and orange destination marker at upper-right. Source: 1254×1254 PNG, resized to 1024×1024 via `sips --resampleHeightWidth`. Final metadata: 1024×1024, sRGB, RGB no alpha (Opaque: true, BitsPerComponent: 8). Intentionally different from `AppLaunchBase.png` (which is the route-free `image2.png`). Contents.json unchanged (single `AppIcon-1024.png` entry). AccentColor unchanged (sRGB safety-orange 0.93/0.55/0.13). Launch screen: Xcode-generated blank (`INFOPLIST_KEY_UILaunchScreen_Generation = YES`). Build: **BUILD SUCCEEDED** clean. |
| 5-6 | [~] TestFlight build preparation | P2 | All code-side prep complete; only manual Xcode archive + App Store Connect upload remain. **Added**: `현장 안전 지킴이/현장 안전 지킴이/PrivacyInfo.xcprivacy` declaring NSPrivacyTracking=false, empty TrackingDomains, empty CollectedDataTypes, and one NSPrivacyAccessedAPIType entry — UserDefaults with reason CA92.1 (App functionality). File auto-picked-up via PBXFileSystemSynchronizedRootGroup. **Verified existing**: MARKETING_VERSION=1.0, CURRENT_PROJECT_VERSION=1, PRODUCT_BUNDLE_IDENTIFIER=com.safetywalk.app, DEVELOPMENT_TEAM=PV6R85F5UM, CODE_SIGN_STYLE=Automatic, INFOPLIST_KEY_NSCameraUsageDescription + NSPhotoLibraryUsageDescription (Korean), UILaunchScreen_Generation=YES. **Network/analytics scan clean**: no URLSession/URLRequest/Firebase/Sentry/Crashlytics/CoreLocation/AdSupport/AppTrackingTransparency anywhere in the source. **Required-reason API audit**: only UserDefaults (CA92.1); no file timestamp / boot time / disk space APIs used. **AppIcon**: 5-5 placeholder is mechanically valid (1024×1024, sRGB, no alpha) — must be replaced with final artwork before App Store review. **Remaining manual steps** (require Xcode UI + Apple ID): (1) Xcode → Product → Archive; (2) Organizer → Distribute App → App Store Connect → Upload; (3) App Store Connect → invite internal TestFlight testers. Status `[~]` because those three steps must be done by the human operator |
| 5-7 | [x] Phase 3 deferred UX polish bundle | P2 | All five sub-items landed in one PR. (a) Address: replaced `addressBinding` with `@State addressDraft` initialized once via `.onAppear`/`didLoadDrafts` guard — TextField now binds to draft (spaces preserved while typing); trim/normalize only at commit time. (b) Area name: `AreaEditRow` now uses `@State draft` + `.onChange(of: draft)` — non-empty trimmed values commit to `area.name`; empty input preserves the last valid `area.name`. (c) onDisappear save: replaced unconditional `try? save()` with `commitSiteEdits()` — empty/whitespace `nameDraft` does NOT overwrite `site.name`; address commits as nil when blank. (d) Area count capsule: `SiteRowLabel` now shows `mappin` SF Symbol + count in the capsule (no new locale key — icon-only). (e) Scope footer: `InspectionScopeSelectionView` lifts `beginButton` (summary + Begin) into `.safeAreaInset(edge: .bottom)` with `.bar` background — stays pinned above the home indicator on small screens; List scrolls underneath; Clear All / Recommended Defaults disable/enable logic unaffected. No new locale keys, no model changes; type-check clean |
| 5-8 | [x] App appearance mode setting (System / Light / Dark) | P2 | New `AppearanceMode` enum added to `Models/Enums.swift` (pure Foundation: `case system, light, dark`, `String` raw, `CaseIterable`, `Identifiable`, `Hashable`). SwiftUI `colorScheme: ColorScheme?` mapping lives in `SafetyWalkApp.swift` (extension below the `App` struct) — keeps Enums.swift SwiftUI-free. Storage: `@AppStorage("com.safetywalk.appearanceMode")` (RawRepresentable+String init), default `.system`. Same key is bound by `SafetyWalkApp` (root) and `SettingsView` (Picker) — SwiftUI republishes the change, so the whole tree re-renders without restart. Root application: `ContentView().preferredColorScheme(appearanceMode.colorScheme)` on `WindowGroup`; `nil` → follow device. Settings UI: new `appearanceSection` between Region and Manage Sites — segmented `Picker` (matches Region styling) with three options. **PDF light-mode preservation**: `InspectionReportView` already pins `.environment(\.colorScheme, .light)` + `.background(Color.white)` + `.foregroundStyle(Color.black)` (line 60), and `InspectionExportService` uses `ImageRenderer(content: report)` (detached SwiftUI hierarchy with no scene tint inheritance) — no change needed. 4 new localization keys: `settings.appearance` ("Appearance" / "화면 모드"), `appearance.system` ("System" / "시스템"), `appearance.light` ("Light" / "라이트"), `appearance.dark` ("Dark" / "다크"); plutil OK on both files; key parity verified. Build: `xcodebuild -sdk iphonesimulator Debug` → **BUILD SUCCEEDED** clean (no compile/link warnings on the new code). No SwiftData model, checklist template, export logic, AppIcon, LaunchScreen, or networking changes |
| 5-9 | [x] Delete inspection records | P2 | `InspectionDetailView` now owns the destructive flow — applies to both in-progress and completed inspections. Added `@Environment(\.modelContext)` + `@Environment(\.dismiss)` + `@State showDeleteConfirm` + a `deleteButton` placed at the bottom of the scroll content (below hazardsSection, after Share/Continue/Checklist). `.bordered` button with `Color.red` tint + `trash` SF Symbol + role `.destructive`; minHeight 50 to meet the 44pt rule. Confirmation: standard SwiftUI `.alert` with title `inspection.deleteTitle`, message `inspection.deleteMessage`, Cancel (role `.cancel`, reuses `common.cancel`) + Delete (role `.destructive`, reuses `common.delete`). **Cascade**: relies on existing `Inspection.@Relationship(deleteRule: .cascade)` for both `items` and `hazards` (Inspection.swift:17-18) — calling `modelContext.delete(inspection)` removes ChecklistItem and Hazard rows in one transaction. **Photo cleanup**: snapshots `inspection.items.compactMap(\.photoPath) + inspection.hazards.map(\.photoPath)` *before* `modelContext.delete(inspection)` (so the cascade doesn't lose the paths), saves the context, then `try? PhotoStorageService().delete(relativePath:)` per path — failures are silent so an orphaned file never blocks the SwiftData delete. **Navigation**: no new `NavigationStack`, no new `@Query`, no depth-2+ destination. `dismiss()` pops back to whichever parent pushed the detail (`InspectionView` or `HistoryTabView` via `.navigationDestination(for: Inspection.self)`) — both parents own a parent-level `@Query` that automatically refetches and removes the deleted row, so the row is gone from the list when the user lands. The pre-existing `.navigationDestination(isPresented: $showChecklist)` to `ChecklistView` is untouched. 3 new localization keys (EN+KO): `inspection.delete` ("Delete Inspection" / "점검 기록 삭제"), `inspection.deleteTitle` ("Delete Inspection?" / "점검 기록을 삭제하시겠습니까?"), `inspection.deleteMessage` (user-data wording only — no legal/compliance/retention language). plutil OK; key parity verified. Build: `xcodebuild -sdk iphonesimulator Debug` → **BUILD SUCCEEDED** clean. No model fields, checklist templates, export/PDF logic, or networking changed; no swipe-to-delete added (deferred — detail-screen path is the only entry point per task scope) |
| 5-11 | [x] Reset Local Data: delete all inspection/business data + evidence photos, preserve preferences | P1 | Settings → "Data Management" section → "Reset Local Data" button (role `.destructive`, `trash` SF Symbol). Confirmation `.alert` explains what is deleted (all Site/Area/Inspection/ChecklistItem/Hazard records + EvidencePhotos) and what is kept (inspector name, region, appearance mode, onboarding status). Deletion order: Inspection first (cascade removes ChecklistItem + Inspection-owned Hazard), then remaining Hazard (catches standalone/orphaned), then Site (cascade removes Area); `modelContext.save()` commits; then `PhotoStorageService().deleteAllEvidencePhotos()` removes every file under Documents/EvidencePhotos/. Photo failure does not roll back SwiftData deletion. Error alert shown on any failure. `@Environment(\.modelContext)` used in SettingsView (root tab view — safe, no `@Query`). No new NavigationStack. `import SwiftData` added to SettingsView.swift. `PhotoStorageService.deleteAllEvidencePhotos()` added (`nonisolated`, enumerates folder contents, silently returns if folder absent). 6 new localization keys EN+KO: `settings.dataManagement`, `settings.resetLocalData`, `reset.confirmTitle`, `reset.confirmMessage`, `reset.confirmAction`, `error.resetFailed`. plutil OK on both files. Build: **BUILD SUCCEEDED** clean. Preserved: `com.safetywalk.inspectorName`, `com.safetywalk.regionProfile`, `com.safetywalk.appearanceMode`, `com.safetywalk.hasCompletedOnboarding` — not touched by reset. |
| 5-14 | [x] Reopen setup screen from Settings | P2 | **SettingsView.swift**: added `@AppStorage("com.safetywalk.hasCompletedOnboarding")` + `@State private var showReopenSetupConfirm`; new `reopenSetupSection` (Section "App Setup" / "앱 설정", `Button` labelled "Reopen Setup" / "초기 설정 다시 보기", `arrow.counterclockwise` SF Symbol); confirmation `.alert` (title `setupReopen.confirmTitle`, message `setupReopen.confirmMessage`, Cancel + Reopen buttons); confirm action sets `hasCompletedOnboarding = false` — ContentView's AppStorage gate responds automatically, no navigation code needed. **OnboardingView.swift**: added `@AppStorage("com.safetywalk.inspectorName") private var savedInspectorName`; `onAppear` now prefills `inspectorName = savedInspectorName` if field is empty — existing name surfaces when screen is reopened; region prefill unchanged (already used `RegionProfileStore.get()`). **Polish**: `headerSection` now starts with `Image(systemName: "shield.fill")` (44pt, `.tint` color) — entry-screen feel using a built-in SF Symbol, no new assets added. **Data preservation**: setting `hasCompletedOnboarding = false` does not touch Sites, Areas, Inspections, ChecklistItems, Hazards, photos, inspector name, region, or appearance mode — all existing data remains intact. **No login/account vocabulary added**. **Localization**: 5 new EN+KO keys — `settings.appSetup`, `settings.reopenSetup`, `setupReopen.confirmTitle`, `setupReopen.confirmMessage`, `setupReopen.confirmAction`; plutil OK on both files. **Build**: `xcodebuild … clean build` → **BUILD SUCCEEDED**, 0 errors, 0 stale warnings. |
| 5-13 | [x] Change app display name to SafetyWalk | P2 | Added `INFOPLIST_KEY_CFBundleDisplayName = SafetyWalk;` to both Debug and Release build configurations in `project.pbxproj`. Project uses `GENERATE_INFOPLIST_FILE = YES` (no separate Info.plist file); `PRODUCT_NAME` remains `$(TARGET_NAME)` ("현장 안전 지킴이") so the Xcode target/bundle name is unchanged. `PRODUCT_BUNDLE_IDENTIFIER = com.safetywalk.app` unchanged. AppIcon unchanged. Localization keys unchanged. SwiftData models unchanged. **Verification**: `python3 plistlib.load` on built `/tmp/safetywalk_build/Debug-iphonesimulator/현장 안전 지킴이.app/Info.plist` → `CFBundleDisplayName: SafetyWalk`, `CFBundleIdentifier: com.safetywalk.app`. Build: **BUILD SUCCEEDED** clean (iPhone 17 Pro Max / iOS 26.4 simulator). **Manual verification needed**: uninstall + reinstall app in simulator and confirm home-screen label shows "SafetyWalk". |
| 5-12 | [x] Post-QA fix: Remove InspectionDetailView pushed @Query (final QA Medium M1) | P1 | Final post-polish QA audit (4 parallel Explore agents covering 14 areas) flagged one Medium issue: `InspectionDetailView` (pushed via `.navigationDestination(for: Inspection.self)` from HomeView/HistoryTabView/InspectionView — depth-2+) still owned `@Query private var items: [ChecklistItem]` with `init { _items = Query(filter: #Predicate { item.inspectionId == id }, sort: [SortDescriptor(\\.sortOrder)]) }`. This violates `/navigation-qa` Rule A (depth-2+ pushed destination must not own `@Query(filter: #Predicate)`). The app was not freezing in practice — 3-M7's specific freeze trigger was `.navigationDestination(isPresented:)`, while this case uses `.navigationDestination(for:)` — but the pattern is the documented fragile one and was the last unfixed instance after 3-M7 cleaned up `ChecklistView` + `InspectionSummaryView`. **Fix**: removed `@Query private var items` declaration and the custom `init(inspection:)` block; replaced with a relationship-backed computed property `private var items: [ChecklistItem] { inspection.items.sorted { $0.sortOrder < $1.sortOrder } }` — identical pattern to `ChecklistView.swift:16-18` and `InspectionSummaryView.swift:13-15`. **Preserved unchanged**: grouped checklist display (`groupedItems`), all derived counts (`passCount`/`failCount`/`naCount`/`checkedCount`), Continue button (in-progress only), Share Report button (completed only), Delete Inspection flow (snapshot photoPaths → `modelContext.delete(inspection)` → `save()` → `PhotoStorageService().delete(...)` per path → `dismiss()`), hazards section, header/progress cards, navigation alert. No new `@Query`. No new `NavigationStack`. No new `.navigationDestination`. No SwiftData model field change. No localization change. No export/PDF/share/intro/asset change. `import SwiftData` kept (still needed for `@Environment(\\.modelContext)`, `modelContext.delete`, `modelContext.save`). **Source scan**: `grep -n "@Query\\|_items = Query\\|#Predicate" InspectionDetailView.swift` → only one match at line 15, which is the comment documenting the `/navigation-qa` rule the fix follows (no actual `@Query` code). **Build**: `xcodebuild -sdk iphonesimulator -destination "generic/platform=iOS Simulator" -configuration Debug SYMROOT=/tmp/safetywalk_build` → **BUILD SUCCEEDED** clean. **Simulator validation** (iPhone 17 Pro Max / iOS 26.4 booted, fresh install + AppStorage seed): Home shows in-progress + completed cards → tap in-progress card → InspectionDetailView renders all sections (header, progress 1/1/0 of 2/3, 점검 계속 button, 점검 항목 grouped by 일반 안전/보호구/추락 위험, 연결된 위험요인 with risk dot, 점검 기록 삭제) → back → re-open ×3 consecutive cycles, no freeze, no hang, all elements re-render cleanly → History tab → tap completed inspection → InspectionDetailView shows 완료 badge + 시작/완료 timestamps + 2/0/0 of 2/2 progress + **보고서 공유** button (ShareReportButton, completed-only) + grouped 화재 위험 items (모두 pass) + 점검 기록 삭제 → tap delete button → confirmation alert renders correctly with localized title/message + 취소/삭제 buttons. After ~10 navigation pushes/pops total, no UI hang observed |
| 5-10 | [x] Route-reveal intro — white/silver shield launch base image + SwiftUI vector route/marker overlay | P2 | **Sixteenth revision: route restyled with AppIcon-like layered strokes and waypoint nodes.** Single thin stroke replaced with 4-layer route: shadow (lineWidth 9, black 0.10 opacity, blur 3, offset y:2) + outer white rim (lineWidth 7, white 0.85) + main blue-gray body (lineWidth 5, Color(0.31,0.40,0.46) opacity 0.92) + inner white highlight (lineWidth 1.2, white 0.28). Start node replaced with 3-layer circles: 16pt white outer (shadow) + 10pt blue-gray ring + 6pt dark inner dot at p0. Mid-route waypoint at B(0.50)=(139,142): same style, 14/9/5pt, opacity 0.70, always visible. Destination marker enhanced with 18pt white rim + shadow under 14pt orange fill. Route points unchanged. Animation, Reduce Motion, intro size, AppLaunchBase, AppIcon unchanged. Build: **BUILD SUCCEEDED** clean. **Previous (Fifteenth revision): route tuned against AppIcon design; app-icon-style start node added.** Route points micro-adjusted to match AppIcon path: p0=(104,174), p1=(96,130), p2=(176,156), p3=(192,106). Added small gray circular start node (9pt, fill white:0.87, stroke muted blue-gray 0.55 opacity, lineWidth 1.0, overall opacity 0.80) at p0 in ZStack — matches AppIcon departure dot. Stroke opacity 0.42→0.46 (slightly more visible, still soft). Marker kept at 14pt warm orange. Animation, Reduce Motion, intro size, AppLaunchBase, AppIcon unchanged. Build: **BUILD SUCCEEDED** clean. **Previous (Fourteenth revision): route shape tuned inward and softened to match app icon style.** Route points repositioned to sit inside the shield panels: p0=(108,176), p1=(92,136), p2=(182,160), p3=(194,110). Stroke lineWidth 2.0→1.6pt, opacity 0.52→0.42 (muted blue-gray, softer, less visually heavy). Marker kept at 14pt. Animation, Reduce Motion, intro size, AppLaunchBase, AppIcon unchanged. Build: **BUILD SUCCEEDED** clean. **Previous (Thirteenth revision): intro mark enlarged to 288pt.** `introMarkSize` increased 264→288pt. Route points scaled ×(288/264): p0=(95,190), p1=(74,125), p2=(193,178), p3=(203,100). CornerRadius 59→65 (≈288×0.225). Marker 13→14pt. Stroke lineWidth kept at 2.0pt. Animation phases, Reduce Motion, tab replay, AppLaunchBase, AppIcon unchanged. Build: **BUILD SUCCEEDED** clean. **Previous (Twelfth revision): intro mark enlarged to 264pt.** `introMarkSize` increased 224→264pt. Route points scaled ×(264/224): p0=(87,174), p1=(68,115), p2=(177,163), p3=(186,92). CornerRadius 50→59 (≈264×0.225). Marker 12→13pt. Stroke lineWidth kept at 2.0pt (visual weight preserved). Animation phases, Reduce Motion, tab replay, AppLaunchBase, AppIcon unchanged. Build: **BUILD SUCCEEDED** clean. **Previous (Eleventh revision): AppLaunchBase replaced with clean image3.png artwork.** `AppLaunchBase.imageset/AppLaunchBase.png` replaced with `image3.png` (found at project root, SHA256=ece82722…). New image: 1254×1254 PNG, RGB no alpha, pure white background with white/silver 3D shield and 2×2 grid, **no route line, no orange marker, no waypoint dots**. ContentView.swift unchanged — SwiftUI vector route + BezierPositionModifier overlay continues as before. AppIcon.appiconset unchanged. Build: **BUILD SUCCEEDED** clean. **Previous (Tenth revision): intro mark enlarged to 224pt; route points pulled inward; route weight reduced; AppLaunchBase background cleaned to white.** (A) `introMarkSize` increased 192→224pt. Route points replaced with inward-pulled values: p0=(74,148), p1=(58,98), p2=(150,138), p3=(158,78) — marker now travels inside the shield body, not edge-to-edge. CornerRadius 43→50 (≈224×0.225). Stroke lineWidth 2.6→2.0, opacity 0.90→0.52 (muted graphite, reduces visual clutter). Marker kept 12pt. Animation phases, Reduce Motion, tab replay unchanged. (B) AppLaunchBase.png regenerated from source image using Python/Pillow: all pixels with min_channel ≥ 210 set to pure white — eliminates the gray rounded-rectangle card background while preserving the shield's structural elements: outer border ring (~193-200), 2×2 grid dividers (~199-201), bottom drop shadow (~151-175). The shield floats on the white launch screen without a visible card. No flood-fill connectivity used; the simple threshold approach avoids grey-fringe artifacts. Final image: 1254×1254 PNG, RGB no alpha, sRGB, 332KB. AppIcon.appiconset SHA1 verified unchanged. Build: **BUILD SUCCEEDED** clean. **Previous (Ninth revision):** `introMarkSize` increased from 160→192pt. Route control points scaled ×1.2 (p0=(57.6,129.6), p1=(40.8,79.2), p2=(132.0,124.8), p3=(141.6,62.4)). CornerRadius 36→43 (≈192×0.225, iOS icon rounding ratio). Stroke lineWidth 2.2→2.6. Marker circle 10→12pt. Animation phases, Reduce Motion, tab replay, AppLaunchBase image unchanged. Build: **BUILD SUCCEEDED** clean. **Previous (Eighth revision): AppLaunchBase replaced with white/silver shield artwork.** `AppLaunchBase.imageset/AppLaunchBase.png` replaced with `image2.png` (found at project root). New image: 1254×1254 PNG, white/silver photo-realistic shield with 2×2 grid, iOS app-icon rounded background, **no static route, no orange marker, no waypoint dots** — fully clean for vector animation overlay. `hasAlpha: no`, bitsPerSample: 8, compiled by Xcode as sRGB Opaque 1254×1254 in Assets.car. ContentView.swift unchanged: `Image("AppLaunchBase")` + `ShieldRoute` + `BezierPositionModifier` vector overlay continues as before. AppIcon.appiconset unchanged. Build: **BUILD SUCCEEDED** clean. **Previous revision (Seventh):** **Seventh revision: hybrid image-base + vector overlay approach.** Reason for change from fully-vector: the 100% SwiftUI shield looked different from the home-screen AppIcon-1024.png, breaking the "icon opening" illusion. **New approach**: `LaunchIntroMark` uses `Image("AppLaunchBase")` as the visual base (matching AppIcon style), with `ShieldRoute` + `BezierPositionModifier` vector overlay drawn on top in the same 160×160 coordinate space. **AppLaunchBase asset**: new `Assets.xcassets/AppLaunchBase.imageset/` — 512×512 PNG generated by CoreGraphics (`/tmp/gen_base.swift`). Draws the same dark navy background gradient, shield shape, and 2×2 grid lines as AppIcon-1024.png but with **no static route and no orange marker** — the canvas is clean so the vector animation reads without visual clutter. Color match: bg gradient (0.13,0.16,0.21)→(0.09,0.11,0.16); shield fill (0.16,0.20,0.26)→(0.12,0.15,0.21); grid alpha 0.45. `hasAlpha: no`, sRGB. **Removed from ContentView**: `ShieldShape` and `ShieldGridLines` structs (replaced by the image). **Kept**: `ShieldRoute: Shape`, `BezierPositionModifier: AnimatableModifier`, all route constants. **Clipping**: `.clipShape(RoundedRectangle(cornerRadius: 36, style: .continuous))` on the 160pt ZStack (cornerRadius 36 ≈ 160 × 0.225, matching iOS icon superellipse proportions) — clips the dark-navy square corners so the intro mark visually matches the rounded home-screen icon shape. **Base image static content**: AppLaunchBase.png contains NO static route or orange marker — the vector overlay is the only route drawn. AppIcon-1024.png (home-screen icon) still contains a baked-in static route/marker, which is intentional for icon legibility. **Route control points** unchanged: p0=(48,108), p1=(34,66), p2=(110,104), p3=(118,52). **Timings** unchanged: Phase 1 easeOut 0.16s + Phase 2 easeInOut 0.40s + Phase 3a easeOut 0.10s + Phase 3b easeIn 0.06s + Phase 4 easeInOut 0.32s = **1.04s**. Reduce Motion: opacity-only 0.40s. AppIcon.appiconset unchanged. No SwiftData, localization, export/PDF, LaunchScreen, or template changes. Build: **BUILD SUCCEEDED** clean. Simulator validation: cold launch confirmed — (1) rounded dark-navy icon appears, (2) route draws progressively with Bézier curve, (3) orange marker follows vector path to upper-right destination, (4) Phase 4 expands outward into OnboardingView. |

---

## QA Session Log

| Date | Scope | Result |
|---|---|---|
| 2026-06-12 | Pre-TestFlight final QA pass (supports 5-6) | See below |

**2026-06-12 — Final QA pass (runtime + static), iPhone 17 Pro / iOS 26.5 simulator, Debug build BUILD SUCCEEDED clean (0 warnings).**

Runtime-verified (screenshot evidence, real simulator):
- Cold launch, no crash. Route-reveal intro plays once. Onboarding gate (fresh install → OnboardingView; inspector name + region + disclaimer, no login vocabulary).
- All 5 tabs render (Home / Inspection / Hazards / History / Settings), each with designed empty states.
- **Navigation freeze paths CLEAR**: Home → InspectionDetailView (depth-2) → ChecklistView (depth-3) push all smooth, no freeze. Confirms 3-M7 / 3-M8 / 5-12 fixes hold at runtime — the entire historical freeze chain works.
- Full Start Inspection flow: site create+select, area, template (region=KR auto-preselected, JSON loaded 14cat×42), scope selection (3-M10 recommended-defaults 5 categories, exclusion wording correct). Text entry works.
- Settings renders with seeded inspector name + region prefilled.

Static-verified: plutil EN/KO OK + key parity 256=256; PrivacyInfo present/valid; no network/analytics/tracking; no login/auth vocabulary; disclaimer text correct; depth-2+ pushed views (ChecklistView/InspectionDetailView/InspectionSummaryView) own no @Query; cascade deleteRule present; reset preserves AppStorage; enums all String rawValue.

Source-verified only (runtime BLOCKED by automation tooling — see safetywalk-qa-guardrails §10): F-04 Add Hazard `.sheet` from checklist, item marking, completion/summary, share/PDF (item 11), delete inspection (7), reset (8), appearance persistence (10), reopen-setup action (4), enum-seeded hazard decode (9). These sit behind SwiftUI `List` rows, which do not respond to System Events synthetic clicks; Quartz CGEvent blocked by missing Accessibility permission. Add Hazard sheet was manually verified at 3-CLOSE.

Findings: (1) IPHONEOS_DEPLOYMENT_TARGET = 26.4 in project.pbxproj — conflicts with the documented "iOS 17+" in CLAUDE.md/CONTEXT.md. Flagged for owner decision (not changed — model/config change needs approval).

design-visual-qa (captured screens): Hierarchy / Spacing / Typography / Color-contrast / Alignment / SafetyWalk-intent all PASS on Onboarding, Home, 5 tabs, inspection flow, Detail, Checklist. Risk-level color coding (Low/Med/High) not runtime-confirmed (hazard screens blocked). Direction/taste left to owner.

---

## Architecture Reference

```
SafetyWalk/
├── App/
│   └── SafetyWalkApp.swift         # ModelContainer setup, app entry
├── Models/
│   ├── Site.swift
│   ├── Area.swift
│   ├── Inspection.swift
│   ├── ChecklistItem.swift
│   ├── Hazard.swift
│   ├── ChecklistTemplate.swift      # Non-persistent, loaded from JSON
│   └── Enums.swift                  # All domain enums
├── ViewModels/
│   ├── HomeViewModel.swift
│   ├── InspectionViewModel.swift
│   ├── HazardViewModel.swift
│   └── SettingsViewModel.swift
├── Views/
│   ├── Home/
│   ├── Inspection/
│   ├── Hazard/
│   ├── History/
│   └── Settings/
├── Services/
│   ├── PhotoStorageService.swift
│   ├── CameraService.swift
│   └── ChecklistTemplateLoader.swift
├── Resources/
│   ├── Localization/
│   │   ├── en.lproj/Localizable.strings
│   │   └── ko.lproj/Localizable.strings
│   └── Templates/
│       ├── checklist_korea.json
│       └── checklist_global.json
└── Utilities/
    └── LocalizationKey.swift
```

---

## Data Model Diagram

```
Site ──< Area
  │
  └──< Inspection
          │
          ├──< ChecklistItem ──> (optional) Hazard
          │
          └──< Hazard
                  │
                  └── photoPath (file in Documents/)
```

---

## Known Risks

| Risk | Mitigation |
|---|---|
| PDF generation complexity on iOS | Build image-based share first; PDF as enhancement |
| SwiftData iOS 17 minimum | Confirmed acceptable; older device support not required |
| Photo storage disk usage | Compress to 1024px on save; alert user if storage low |
| Region-specific legal content creep | Enforce: legal text is data only, never in app logic |
| Scope creep on checklist complexity | MVP = flat Pass/Fail/NA; hierarchy is P2 |
