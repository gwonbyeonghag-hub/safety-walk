# DESIGN_DIRECTION.md — SafetyWalk / 현장안전 지킴이

> 이 문서는 SafetyWalk의 **디자인 방향(정체성)** 을 고정한다. 위키
> `01_코딩 학습/02_wiki/frontend-design-agent-workflow.md`의 **주제 기반 디자인(theme-based design)**
> 방법론을 SafetyWalk에 적용한 결과다. 디자인을 실행자에게 통째로 위임하면 "AI 템플릿 미감"이
> 나오므로(주제 없이 자동 생성), 방향은 여기서 **주제로부터** 정해둔다.
> **WO-5(디자인 폴리시 + 리포트)와 `design-visual-qa` 게이트가 이 문서를 기준으로 삼는다.**
> 오너 확정: 2026-06-27 (브랜드 톤·시그니처·팔레트).

---

## 0. 한 줄 정체성
**현장에서 한 손으로 위험을 기록하는, 신뢰할 수 있고 명료한 안전 도구.** 정체성의 중심 언어는 **위험등급 컬러 시스템**이고, 토대는 **Apple HIG(절제 + liquid glass)** 다.

---

## 1. 주제 기반 디자인 7개 패스 (확정)

### Subject — 무엇을 위한 화면인가
현장 안전점검·위험성평가를 **현장에서 즉시** 기록하는 도구. 범용 폼/대시보드 앱이 아니라, 거친 현장에서 쓰는 전용 도구.

### Audience — 누가 쓰는가
- iOS: 현장 안전관리자 + 현장 감독자. **기술 숙련도 다양**, 한 손 사용, 장갑·햇빛·먼지 환경.
- macOS: 매니저(여러 현장 조망·리포트 출력).

### Page job — 화면별 "단 하나의 일"
| 화면 | 단 하나의 일 |
|---|---|
| iOS 홈 | 지금 현장 상태 한눈에 + **점검 바로 시작** (대시보드 아님) |
| 점검 / 위험성평가 입력 | 현장에서 **한 손으로 빠르게 기록** |
| macOS 대시보드 | 여러 현장의 **위험·미조치를 조망** |
| 리포트 | **제출 가능한 공식 문서**를 깔끔히 출력 |

### Palette — navy + orange 유지·정제 (위험색은 의미 전용)
**원칙: 색을 "쿨 = 앱/조작" vs "웜 = 위험"으로 가른다.** 그래야 사용자가 "주황 버튼"과 "중위험 주황"을 헷갈리지 않는다(기존 액센트-오렌지 ↔ 중위험-오렌지 충돌 해소).

- **쿨(브랜드·인터랙티브)**: **navy** 계열 = 앱 크롬·구조, 그리고 **버튼/링크/선택 상태 등 액센트는 navy/blue 쪽**으로. (오렌지를 액센트로 쓰지 않는다 — 오렌지는 위험 의미에 양보.)
- **웜(위험 의미 전용 = 시그니처)**: 위험등급 램프. **낮음 = 딥 골드**(`riskLow` 0.70/0.52/0.00 — 밝은 system yellow 아님, 대비 확보·인쇄 골드와 일치), **보통 = 주황**, **높음 = 빨강**. 이 램프는 **위험을 뜻할 때만** 쓴다(장식 금지).
- **green = 완료·안정·적합 전용** (기존 규칙 유지).
- 라이트/다크 적응(이미 launch·accent가 적응형).
- 아이콘의 오렌지 체크는 **브랜드 마크**(앱 밖 정체성); 앱 안에서 오렌지는 위험 의미.

### Type — Apple HIG 네이티브
- **SF Pro**(시스템). 역할: **display**(타이틀) / **body**(본문) / **utility**(캡션·라벨).
- **숫자(위험점수·카운트·날짜)는 monospaced digits** — 표·리스트 정렬. 점수 1–9, 카운트가 흔들리지 않게.
- 폰트 종류 **최소화**(HIG). `Inter` 등 "AI 생성 티" 폰트 쓰지 않음 — 시스템 폰트가 곧 네이티브감.
- Dynamic Type(접근성 글자크기) 지원.

### Layout concept
- **iOS = dense-but-calm 수직 현장 도구**: 44pt+ 한 손 타깃, 가로 스크롤 없음, 홈은 glanceable 위험 레일(대시보드 금지 — CLAUDE.md).
- **macOS = 조망형 대시보드**: 현장별 위험 현황·미완료 시정조치·위험성평가 due·최근 점검. 인쇄/PDF 허브.

### Signature element — **위험등급 컬러 시스템** (대담함은 여기 한 곳)
앱 전체를 관통하는 **단 하나의 시각 언어**: 상/중/하 위험색을 **liquid-glass 칩·레일·밴드**로 일관되게.
- 등장처: 홈 위험 레일 · 위험요인 배지 · 위험성평가 밴드 · 빈도×강도 점수 칩 · 리포트 색 밴드.
- 형태·크기·재질(반투명 glass)을 **통일** → 어디서 보든 "같은 위험 언어". 나머지 UI는 조용·정돈(절제).
- 이게 시그니처이므로, **다른 곳에서 대담한 요소를 추가하지 않는다**(경쟁 금지). 아이콘 방패는 브랜드 마크로 앱 밖에서만.

---

## 2. 자기비판 패스 (통과 확인)
- [x] **주제가 아니어도 똑같이 나오나?** → 아니오. 위험색 시스템·쿨/웜 분리·dense-calm 현장 레이아웃은 "안전 점검"이라는 주제에서 나옴.
- [x] 색·타이포·레이아웃 중 주제에서 나온 게 있나? → 웜 램프=위험 의미, 한 손 dense 레이아웃=현장 사용.
- [x] numbered marker는 실제 순서일 때만? → JSA 작업단계 ①② 만 사용, 그 외 금지.
- [x] 장식이 정보를 돕나? → 위험 칩은 의미를 인코딩(장식 아님).
- [x] 시그니처 2개 이상 경쟁? → 아니오. 위험등급 컬러 시스템 단 하나.
- [x] 모바일/포커스/reduced motion? → 44pt·VoiceOver 라벨·Reduce Motion 대응(런치 인트로에 이미 적용).

---

## 3. 피해야 할 "AI 템플릿 미감" 기본값 (이 프로젝트 맞춤)
주제가 요구하지 않는데 자동으로 나오면 의심:
- warm minimalism(크림+세리프+테라코타) / dark maximalism(검정+산성 녹색) — **쓰지 않음**
- 의미 없는 gradient/orb/hero 장식 — **금지**(현장 도구엔 부적합)
- 의미 없는 01/02/03 마커 — JSA 단계 외 금지
- 과한 애니메이션·hover — 절제
- `Inter` 등 디폴트 폰트 — SF Pro 사용

---

## 4. Apple HIG / liquid glass (토대)
- iOS·macOS 모두 **"네이티브로 느껴지는"** 결과 목표(아이폰 앱을 Mac에 욱여넣는 느낌 X).
- 공유 `SafetyWalkCore` + **플랫폼별 UI**(SwiftUI). 위험 칩의 반투명/머티리얼은 HIG liquid-glass와 결을 맞춤(아이콘과 일관).
- SF Symbols, 다크모드, Dynamic Type, 안전영역 준수.

## 5. UI 문구 (디자인 재료)
- **사용자 언어**(시스템 용어 X): 행동 중심 라벨.
- **같은 동작=같은 단어**(예: 저장/완료 용어 통일).
- **에러·빈 상태 = 분위기 말고 "무슨 일 + 다음 행동"**. 사과·모호 금지.
- EN/KO 키 패리티(`LocalizationKey`). 한국어 문체는 위키 `korean-writing-humanization-harness` 관점 참고.

## 6. 애니메이션
- **`transform` + `opacity`로만** 움직인다(`top/left/size` 변경=reflow=끊김 금지).
- 의도 있는 모션 **한두 곳**(예: 위험 칩 상태 전환). 반복적 "아래서 슬라이드인" 남발 금지.
- Reduce Motion이면 모션 제거 + 크로스페이드.

## 7. 리포트 ("보고서 이쁘게")
- 제네릭 표 덤프 금지. **navy 마스트헤드 + 위험색 밴드(시그니처) + 명확한 위계 + monospaced 숫자 + 면책 고지**.
- 양식: **위험성평가표(KR, A4)** · **JHA 워크시트(US, Letter)** · 점검 리포트. (WO-5에서 구현)

---

## 8. WO-5 / design-visual-qa 연결
- **WO-5(디자인 폴리시 + 리포트)** 는 이 문서를 사양으로 삼아 화면·리포트를 다듬는다.
- `design-visual-qa` 게이트: 각 화면을 렌더→스크린샷→**§2 자기비판 + §3 금지 기본값**으로 비평하는 루프.
- 새 화면/리포트는 §1 팔레트·타입·시그니처·레이아웃을 따른다. 벗어나려면 이 문서를 먼저 고친다(주제 기반 유지).

---

## 참고
- 방법론: `01_코딩 학습/02_wiki/frontend-design-agent-workflow.md` (주제 기반 디자인·시각 QA·네이티브 디자인 시스템)
- 규칙: `CLAUDE.md`(iOS 현장도구/대시보드 금지, macOS 대시보드, 위험색=기록) · `DOMAIN_TERMS.md`(위험등급)
