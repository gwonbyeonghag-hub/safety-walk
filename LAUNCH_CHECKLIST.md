# LAUNCH_CHECKLIST.md — SafetyWalk / 현장안전 지킴이

Launch-readiness review (2026-06-12). The app is **functionally ~complete**
(70/72 TASKS done, clean build, core navigation freeze paths verified at runtime),
but it is **not yet submittable**. Close P0 before anything else; P0 + P1 before
App Store submit.

How to use: work top-down, one item at a time. Each item has an **acceptance
check** — do not tick `[x]` until that check passes with evidence (build log,
screenshot, or file diff). Owner-decision items are marked 🧭 — confirm with the
human before changing.

---

## Post-checklist changes (2026-06-25) — supersede some items below

Shipped after this checklist was finalized (2026-06-23); these **reverse/update** items below:
- **In-app Language toggle (한국어/English)** implemented in Settings/Onboarding, replacing the standalone region picker → supersedes **P2 "in-app language toggle deferred"** and the **P3 "Global label → KEEP" decision** (line ~114).
- **Non-Korea checklist retargeted to US/OSHA-oriented content** (imperial 6 ft/4 ft, SDS, LOTO, HazCom, machine guarding, walking-working surfaces, etc.); `checklist_global.json` + `DOMAIN_TERMS.md`/`CONTEXT.md`/`PRD.md` updated → resolves the P3 "1.8 m / GFCI" content notes (line ~120).
- **Launch experience — reverted X-style splash → in-app wordmark intro with FILLED letters (2026-06-26)** — the static X-style navy+logo splash was **reverted**: `LaunchBackground` is back to **adaptive white/black** (Any = white, Dark = black), `Info.plist` `UILaunchScreen` drops `UIImageName` (keeps `UIColorName` only), and the `LaunchLogo` imageset is **deleted**. So the static launch is again plain **system color, no logo**. `ContentView.swift` restores the in-app **"SafetyWalk" wordmark intro**, but the glyphs are now **solid-filled** (`WordmarkShape.fill(ink)` revealed left→right by a growing `.mask`) instead of the earlier outline `.trim().stroke()`; orange underline grows on the same progress; ink adapts (white in dark, dark ink in light); plays once, ~1.3s, Reduce Motion = no draw + crossfade. Runtime-verified in **both light and dark**: filled letters reveal L→R then hand off to content. `BUILD SUCCEEDED` 0/0, `plutil` Info.plist OK, no `LaunchLogo`/navy residue, no asset warnings.
- **App icon replaced (2026-06-26)** — new Liquid-Glass-style shield + orange check on navy glass (AI-generated reference, resized to a compliant 1024 square / no-alpha; old shield+route icon backed up at `/tmp/AppIcon-1024.orig.png`). Builds with no icon warnings; renders on the simulator home screen.
- **EN/KO key parity now 300=300** (was 280).
- `APP_STORE_SUBMISSION.md` metadata updated to match (2026-06-25). See `SUBMISSION_RUNBOOK.md` for the human submission steps.

---

## P0 — Blockers (must fix before submit)

- [x] **Deployment target iOS 26.4 → 17.0 — FIXED (2026-06-12)** 🧭
  - Where: `현장 안전 지킴이.xcodeproj/project.pbxproj` → `IPHONEOS_DEPLOYMENT_TARGET` (was 26.4 in 4 configs: main app Debug/Release + test targets).
  - Why: CONTEXT/PRD say "iOS 17+". 26.4 was auto-set to the SDK version. Field users have older devices.
  - Fix applied: owner confirmed 17.0; changed all 4 occurrences to `IPHONEOS_DEPLOYMENT_TARGET = 17.0;` via pbxproj text edit (low-risk value change).
  - ✅ Acceptance met: all 4 configs now `= 17.0`. Clean build at target 17.0 → **BUILD SUCCEEDED, 0 errors, 0 warnings, no availability errors** (confirms no unguarded iOS-26-only API). App installs + launches + renders OnboardingView on **iOS 18.6** simulator (lowest installed runtime ≥17 — no iOS 17 runtime installed), no crash / no dyld symbol errors; built app `Info.plist` MinimumOSVersion=17.0 verified. (A true iOS 17 runtime launch can be added by downloading the iOS 17 sim runtime if desired.)

- [x] **Add Hazard — runtime-verified (2026-06-17)**
  - PRD gap found & closed: `HazardRegistrationView` was reachable only from a Fail checklist item (in-inspection), violating F-04/F-01 ("usable outside an inspection"). Added a standalone entry — a **+ toolbar button on the Hazards tab** that opens the existing `HazardRegistrationView` with `inspection = nil` and a site picker (auto-selects the single site). `Hazard.inspectionId` was already optional, so no model change.
  - **Live UI run on simulator (iPhone 17 Pro / iOS 26.5)** — full standalone path passed, each step screenshot- + SwiftData-verified: + button → pick photo → site auto-selected → location/type/risk/desc/status → **Save** → new hazard appears in list with its **real photo thumbnail** and risk badge → open Detail → change corrective-action status (notStarted → inProgress) → status persists immediately (`updatedAt` bumped) → **quit + relaunch** → hazard and changed status survive. Verified `inspectionId == NULL` (standalone) and that it reflects in the Home risk rail + Hazards list with **no crash and no DecodingError**. Photo file persists under `Documents/EvidencePhotos/`. Build SUCCEEDED.
  - Save logic location: lives in the **Views** (`HazardRegistrationView.save()` does insert+save; `HazardDetailView` saves the status change) via `@Environment(\.modelContext)` — consistent with the codebase's existing View-owns-persistence pattern (same as `ChecklistItemRow`). `HazardViewModel.swift` is an unused empty stub → tracked under P2 ("implement it or delete it").
  - Note: the in-inspection entry (Fail item → same `HazardRegistrationView` sheet, `inspection != nil`) exercises the same verified `save()` path; it was not separately UI-driven because its `.sheet`/List-row controls don't respond to synthetic-click automation (simulator limitation, not an app defect — see safetywalk-qa-guardrails §10).

- [x] **Test-target bundle IDs fixed (2026-06-17)**
  - Was: `PRODUCT_BUNDLE_IDENTIFIER = "group.com.Gwon.QuotaDeck.---------------------Tests"` / `...UITests` (2 configs each, leftover from another project).
  - Fix applied (pbxproj only, no app source touched): Tests → `com.safetywalk.app.Tests`, UITests → `com.safetywalk.app.UITests`, in both Debug & Release configs. Main app id `com.safetywalk.app` unchanged.
  - ✅ Acceptance met: `rg "QuotaDeck|---------------------" project.pbxproj` → no matches. `xcodebuild build-for-testing` → **TEST BUILD SUCCEEDED** (app + `현장 안전 지킴이Tests.xctest` + `현장 안전 지킴이UITests.xctest` all compiled, 0 errors). Built bundles verified: `…Tests.xctest` = `com.safetywalk.app.Tests`, `…UITests.xctest` = `com.safetywalk.app.UITests`, main app still `com.safetywalk.app`.

---

## P1 — Verify before launch (high)

- [x] **Permission-denied states (camera & photo library) — verified safe by design (2026-06-17)**
  - Architecture: photo attach in both flows (`HazardRegistrationView`, `ChecklistView`) uses **`PhotosPicker` (PHPickerViewController)** — out-of-process and **permission-less on iOS 14+** (the app never gets direct library access, so there is no `NSPhotoLibraryUsageDescription` prompt and no denial code path). No `PHPhotoLibrary.requestAuthorization`, `UIImagePickerController`, or `AVCaptureDevice` is called from any View. **`CameraService.swift` is unused** (not referenced anywhere) — the live camera is never invoked, so there is no camera-permission runtime path. Saved-photo display reads from the app's own `Documents/EvidencePhotos/` sandbox, which is permission-independent.
  - Runtime-verified on simulator with **camera + photos + photos-add all revoked**: app launches, Home rail + Hazards list render, **existing hazard photos still load** (sandbox read), and **re-grant → relaunch** works — **no crash, no soft-lock, no DecodingError, no permission/PHPicker error in console**. Photo load/save failures are additionally guarded by `showPhotoLoadFailed` / `showPhotoSaveFailed` alerts (task 5-3), so even an I/O failure surfaces an alert rather than crashing.
  - Could not synthetic-click-drive the live `+` → PHPicker pick *under denial* (toolbar-button / picker-cell automation limit — see safetywalk-qa-guardrails §10), but the flow is permission-less by architecture and a successful live pick+save was already verified in P0-2. Not an app defect.
  - Usage strings present (`INFOPLIST_KEY_NSCameraUsageDescription`, `...NSPhotoLibraryUsageDescription`). **Minor follow-up (P2/P3):** the camera usage string is declared but the camera is never used — consider removing it (or keep for a future live-camera feature) to avoid a reviewer question about an unused permission.

- [x] **Share / Export (PDF) — report redesigned + full end-to-end share verified (2026-06-17)**
  - **Design**: `InspectionReportView` is an A4-width (595pt) field-inspection report — signature = masthead + status strip (적합률 / 미처리 위험 / 진행), then quiet bordered tables: 점검 정보(메타데이터), **점검 결과 요약**(적합/부적합/해당없음/미점검/진행률) and a separate **위험요인 요약**(높음/보통/낮음) — the two were merged before and were split this pass — 카테고리별 점검 항목 결과(모든 항목 + 결과 배지 + 비고), 위험요인 및 시정조치 표, 증거 사진 3열 그리드, 면책 고지, 생성 정보 footer. Risk colors follow DOMAIN_TERMS (높음 red / 보통 orange / 낮음 gold-yellow) **always with a text label**, never color-only. No legal-violation wording (disclaimer states the app does not determine compliance). Export-only code touched; no model/app-screen change.
  - **Runtime QA (iPhone 16 Plus / iOS 18.6 sim)**: built **BUILD SUCCEEDED (0 errors/warnings)**; drove the real `InspectionExportService.exportPDF(inspection:)` pipeline (a DEBUG-only seed harness, since removed) for a full inspection (3 categories, mixed pass/fail/NA/unchecked + notes, 3 hazards high/med/low with varied corrective status, evidence photos) and a minimal/empty inspection. Both produced **valid PDFs** (`pdfinfo`: A4 width 595pt; full 595×1148.5, minimal 595×706; Quartz PDFContext). `pdftoppm` PNG render visually confirmed: no text truncation, no column overlap, photos intact, Korean renders cleanly, **empty states designed** (구역 없음 / 완료 "-" / "첨부된 증거 사진이 없습니다." / hazard table hidden when none), color+label meaning consistent, disclaimer present.
  - **Single tall A4-width page** for MVP (no per-page A4 breaks). True 595×842 multi-page pagination is a deliberate **P2 follow-up** (documented as a TODO in `InspectionExportService.exportPDF`) — it needs block-level layout splitting the single-frame `ImageRenderer` path can't express, so deferred to avoid mid-row slicing.
  - **Share-sheet display — verified end-to-end (2026-06-17, iPhone 16 Plus / iOS 18.6 sim)**: a completed inspection was placed in the store, the user tapped 보고서 공유 on its detail screen → `ShareReportButton` ran `exportPDF` and presented the iOS share sheet → **Save to Files completed**, and the saved PDF opened in the Files/Quick Look viewer rendering the full report correctly (screenshot evidence: masthead + status strip 40% 적합률 / 1 미처리 위험 / 5/6 진행, 점검 정보, 결과 요약 적합2·부적합2·해당없음1·미점검1·진행률 83%, 위험요인 요약 높음1, 카테고리별 표, 위험요인·시정조치 표, 증거사진 빈 상태, 면책 고지). The freshly written file `SafetyWalk-<id>.pdf` re-validated with `pdfinfo` (A4 width 595pt, 1 page, Quartz PDFContext). The synthetic-click toolbar/list/sheet automation limit (safetywalk-qa-guardrails §10) was worked around with a single human tap, per the agreed fallback — not an app defect.
  - New localization keys `report.hazardSummary`, `report.progressRate` added to en + ko (parity 279=279, plutil OK).

- [x] **Delete, Reset local data, Appearance (light/dark) — runtime-verified (2026-06-18, iPhone 16 Plus / iOS 18.6 sim)**
  - **Delete (InspectionDetailView)**: with a seeded completed inspection (3 items + 1 hazard + 2 evidence photos) alongside an in-progress inspection (2 items), drove 완료 card → 점검 기록 삭제 → confirm. **Store verified**: completed inspection + its 3 `ChecklistItem`s + its `Hazard` all removed via `@Relationship(deleteRule: .cascade)`; the in-progress inspection + its 2 items + the parent `Site` correctly **retained**; both evidence photo files for the deleted records were cleaned (`performDelete` snapshots paths then `PhotoStorageService.delete`). UI dismissed to Home and updated live (risk rail → 안정, 오늘 2→1, 완료 card removed). No crash / no SwiftData error in console.
  - **Reset local data (Settings → 로컬 데이터 초기화)**: with 1 site / 1 inspection / 2 items + 2 stray evidence photos present, drove 로컬 데이터 초기화 → confirm. **Store verified empty** (0 sites / 0 inspections / 0 items / 0 hazards / 0 areas via `performReset` fetch-and-delete + cascade + `modelContext.save()`); **EvidencePhotos emptied** (0 files; folder intentionally preserved per `deleteAllEvidencePhotos()`); **preferences retained as documented** (`appearanceMode=light`, `hasCompletedOnboarding=1` untouched — reset deletes SwiftData + photos only, never UserDefaults). Relaunch renders the **designed empty state** ("점검 기록이 없습니다 …") on Home, skips onboarding (flag retained), stays in light mode. No crash / no error.
  - **Appearance (Settings → 화면 모드: 시스템/라이트/다크)**: `@AppStorage("com.safetywalk.appearanceMode")` → `SafetyWalkApp.preferredColorScheme`. **Dark** verified across Home / Hazards / Settings (black bg, white text, risk dots red/orange/yellow + 진행중/완료 badges all legible — good contrast); **Light** verified on Home + Settings (white bg, dark text, light-red rail — good contrast); **persists across relaunch** (set value → cold launch → renders in that mode and the segmented picker reflects the stored value). **System** = `colorScheme(nil)` passthrough (follows OS) — covered by design. No screen breakage in either mode.
  - **Automation note**: tab-bar navigation + appearance-pref application were synthetic-click / defaults-driven; the in-app `NavigationLink` cards, `Form`-row buttons, and confirmation `alert` buttons don't take synthetic clicks (safetywalk-qa-guardrails §10), so the Delete/Reset taps were performed by one human tap each per the agreed fallback, with results verified against the SwiftData store + photo files + screenshots. Not app defects. Seed/temp data fully cleaned afterward (app restored to original onboarding state). Build SUCCEEDED; no app source changed.

- [x] **App icon & launch screen — runtime-verified (2026-06-18, iPhone 16 Plus / iOS 18.6 sim)**
  - **App icon**: `AppIcon.appiconset` is a modern single-size **1024×1024** universal asset (`AppIcon-1024.png`). Verified `sips`/PIL: **RGB, no alpha**, full square (corner pixels opaque ~(253,255,255), no transparency/no baked dark corners) — App Store-compliant; iOS applies the rounded mask. `Contents.json` is valid JSON (python-validated; `plutil -lint` doesn't lint JSON asset catalogs). The dark/tinted appearance slots are declared with no filename (empty) → they fall back to the default icon; **build emits no asset warning**. Wired via `ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon`; compiled into the built `Assets.car` (rendition `AppIcon-1024.png`), built `Info.plist` has `CFBundleIconName=AppIcon` + generated `AppIcon60x60`/`AppIcon76x76~ipad` stamps. **Renders on the simulator Home Screen** (fresh `simctl uninstall`+`install`) with label "SafetyWalk". Design fits the domain — a shield (safety/protection) with a quadrant grid and an orange-marked route (the inspection "walk"), no text per HIG.
  - **Launch screen**: configured via `INFOPLIST_KEY_UILaunchScreen_Generation = YES` → built `Info.plist` has `UILaunchScreen = <dict/>` (blank, **system-background** launch screen — no splash/branding/text, so **no localization or flicker risk** per the *Launching* HIG). Cold-launch capture shows the plain launch screen, then the app's in-app SwiftUI shield intro (`LaunchIntroMark`, post-launch, ~1s), then the first screen. **Transition verified both ways**: fresh install → onboarding (현장 안전 지킴이 setup); `hasCompletedOnboarding=YES` → Home (empty state, "점검 기록이 없습니다…"). No breakage in light mode; the launch screen is the system background so it adapts to light/dark. No crash.
  - **Verification**: build **BUILD SUCCEEDED** (no icon/launch warnings); `sips`/PIL alpha+size; `assetutil --info Assets.car`; built `Info.plist` keys; `simctl install`/`launch`/`screenshot`. No app source / asset edits needed — config already correct. Seed/temp state restored to fresh-install afterward.
  - **Minor follow-up (optional, non-blocking)**: the icon background is quite light (near-white shield on cream), giving moderate contrast at small sizes — recognizable mainly via the orange route marker. Not a defect or submission blocker; could be darkened/contrast-boosted in a future polish pass if desired. No dark/tinted icon variants are provided (default icon used for all appearances) — acceptable for MVP.

- [x] **`PrivacyInfo.xcprivacy` content — verified accurate against actual code use (2026-06-17)**
  - **Manifest is correct as-is; no change needed.** Validated against Apple's *Privacy manifest files*, *TN3183 (required-reason APIs)*, and *App Privacy Details*:
    - `NSPrivacyTracking = false` + `NSPrivacyTrackingDomains = []` — no tracking/ATT/AdSupport code anywhere (`rg` clean).
    - `NSPrivacyCollectedDataTypes = []` — **correct**: "collect" per Apple = data **transmitted off device**. The app is offline-first (zero `URLSession`/`URLRequest`/`Network`/`CFNetwork` symbols in the whole target). Inspector name + RegionProfile live in `UserDefaults`; evidence photos live in the app sandbox (`Documents/EvidencePhotos/`, `PhotoStorageService`). The only egress is the **user-initiated iOS Share Sheet** (Save to Files / Mail / etc.), which is exempt — so nothing is "collected." No analytics/crash/third-party SDKs (0 Swift Package refs, 0 embedded frameworks).
    - `NSPrivacyAccessedAPITypes` = **UserDefaults → `CA92.1`** (App functionality, reading/writing the app's own defaults). This is the **only** required-reason API in use. Audited and found **absent**: file-timestamp APIs (no `attributesOfItem`/`creationDate`/`modificationDate`; `contentsOfDirectory(...includingPropertiesForKeys: nil)`), disk-space APIs, system-boot-time APIs, active-keyboard APIs. `CA92.1` is the correct Apple-approved reason for own-app defaults.
  - **Verification**: `plutil -lint PrivacyInfo.xcprivacy` → OK; manifest is **bundled into the built `.app`** (confirmed `…/Debug-iphonesimulator/…app/PrivacyInfo.xcprivacy`, plutil OK, contains `NSPrivacyAccessedAPICategoryUserDefaults` / `CA92.1`). Build SUCCEEDED (no source changed this pass). An Xcode **Privacy Report** is generatable from an archive (Organizer → Generate Privacy Report) and would aggregate this valid, bundled manifest.
  - **Photos & camera (not a manifest defect)**: `PhotosPicker`/`PHPickerViewController` is out-of-process and **permission-less** → no library access, no data collection, correctly absent from the manifest. `NSCameraUsageDescription` + `NSPhotoLibraryUsageDescription` are present in build settings; the photo-library string isn't strictly required by PHPicker (harmless), and the camera string maps to dead code (`CameraService` with `AVCaptureDevice.requestAccess` is **never invoked**). **Camera string cleanup is intentionally deferred** to the P2 "implement-or-delete `CameraService`" item — removing the purpose string while the `AVCapture` symbols are still compiled in is the riskier direction, so the string should be dropped **together with** deleting `CameraService.swift`, not before. None of this affects `PrivacyInfo.xcprivacy`, which is accurate.

---

## P2 — Quality (pre-launch polish)

- [x] **`NSPhotoLibraryUsageDescription` — decision: KEEP (2026-06-18)** — photo attach uses **only SwiftUI `PhotosPicker` (`PHPickerViewController`)** in `HazardRegistrationView` + `ChecklistView` (both `import PhotosUI`). Audited: **no** `import Photos`, `PHPhotoLibrary`, `requestAuthorization`, `PHAsset`, `UIImagePickerController`, or `ALAssetsLibrary`; `otool -L` shows **Photos.framework is not directly linked** (PHPicker runs out-of-process). So the string is **not technically required** and removing it would be runtime-safe (no API reads it; no "missing purpose string" static-analysis risk since there are no Photos-authorization symbols). **Kept anyway** because — unlike the camera string, which described a capability the app entirely lacked — this string ("증빙사진을 첨부하기 위해 사진 보관함에 접근") is **consistent with user-visible behavior** (the app does present a photo picker), so it carries **no "unnecessary permission" reviewer risk**; PHPicker shows **no prompt** regardless, so keeping it adds no user friction; and it gives forward-compatibility if a future non-PHPicker library path is ever added. Removing it would offer no benefit and add a small future-regression risk. No code/pbxproj change. Verified: built `Info.plist` still contains it; `PrivacyInfo.xcprivacy` unchanged (out-of-process picker = no library access / no data collection, correctly absent from the manifest; `UserDefaults`/`CA92.1` only, plutil OK); **BUILD SUCCEEDED**; photo-attach paths unchanged.
- [x] **Dead code: `CameraService.swift` deleted + `NSCameraUsageDescription` removed (2026-06-18)** — the app provides no live-camera feature; photo attach is `PhotosPicker`/`PHPickerViewController` only (`HazardRegistrationView`, `ChecklistView`), which is out-of-process and permission-less. `CameraService` (the sole holder of `AVCaptureDevice`/`import AVFoundation`) was referenced nowhere, and its `CameraPermissionStatus` enum was self-contained, so the file was deleted (auto-removed from the build via the synchronized file group). With the camera symbols gone, `INFOPLIST_KEY_NSCameraUsageDescription` was removed from both Debug & Release configs — done **together** so there's no orphan camera API without a purpose string (nor a purpose string without an API). Verified: `rg` finds **no** `CameraService`/`AVCapture`/`AVAuthorization`/`UIImagePickerController`/`import AVFoundation`; built `Info.plist` has **no** `NSCameraUsageDescription`; the binary **no longer links AVFoundation** (`otool -L`). `NSPhotoLibraryUsageDescription` kept; `PhotosPicker` paths unchanged; `PrivacyInfo.xcprivacy` unchanged (still `UserDefaults`/`CA92.1` only, plutil OK). **BUILD SUCCEEDED** (0 errors/warnings). Closes the camera-string follow-up noted under the P1 permission + PrivacyInfo items.
- [x] **Risk-color language unified to DOMAIN_TERMS (2026-06-18)** — 높음=red / 보통=orange / **낮음=yellow**; green reserved for 완료·안정·적합. Five on-screen `riskColor` helpers (+ one `riskCell` call) mapped 낮음 to `.green`, contradicting DOMAIN_TERMS and the already-correct Home rail (`.yellow`) / PDF (gold) — fixed in place (minimal change, no shared-helper refactor): `HazardDetailView`, `HazardListView` (×2: filter chip + row badge), `HazardRegistrationView`, `InspectionDetailView`, `InspectionSummaryView` → `.low` now returns `.yellow` (matches Home). PDF (`InspectionReportView`) keeps its print-legible gold `Color(red:0.70,green:0.52,blue:0)` — allowed exception. Legitimate green (pass/completed/stable status) untouched; risk labels (낮음/보통/높음) preserved so meaning isn't color-only; no enum/model/logic/PDF-structure change. Verified: `rg` finds no `RiskLevel.low → .green` mapping anywhere; **BUILD SUCCEEDED**; simulator Hazards list shows 낮음=yellow / 보통=orange / 높음=red badges. **Contrast residual resolved (2026-06-18, see next item).**
- [x] **Risk-color accessibility/contrast pass (2026-06-18)** — the bright system `.yellow` for 낮음 had weak contrast in two patterns: **white-on-solid yellow** (HazardRegistrationView selected chip, HazardListView active filter chip — ~1.4:1) and **yellow text/number on light** (HazardListView row badge, HazardDetailView, InspectionDetailView, InspectionSummaryView count, plus Home rail's large 낮음 icon/count). Introduced a single small constant `Color.riskLow = Color(red:0.70,green:0.52,blue:0)` (deeper gold = the PDF print-gold) and pointed all 8 on-screen 낮음 sites at it (`HomeView` railAccent + riskColor, `HazardDetailView`, `HazardListView` ×2, `HazardRegistrationView`, `InspectionDetailView`, `InspectionSummaryView`). White-on-solid rises ~1.4:1 → ~3.4:1 (passes WCAG AA-large for the bold chip text), and gold-on-light/gold-on-dark both read clearly. Meaning still carried by the 낮음/보통/높음 labels (not color-only); 높음=red / 보통=orange and all green (완료·안정·적합) unchanged; no enum/model/filter/PDF-structure change. PDF keeps its own gold literal (same value). Verified: `rg` finds no remaining `.yellow` outside the doc comment; **BUILD SUCCEEDED**; simulator Hazards list checked in **both light and dark** — 낮음=gold / 보통=orange / 높음=red all legible. The selected-chip / active-filter (white-on-gold) cases are toolbar/sheet-gated from synthetic clicks (§10) so verified by the computed contrast lift + the shared constant, not a live tap.
- [x] **Dead code: empty `HazardViewModel.swift` deleted (2026-06-18)** — it was an `@Observable` stub with only TODO comments (no stored state, no methods). `rg "HazardViewModel"` (source, excluding `build/`) found it referenced **nowhere** but its own definition; `Views/Hazard/*` use **no** ViewModel. Hazard persistence is View-owned and untouched: `HazardRegistrationView.save()` does `modelContext.insert(hazard)` + `try? modelContext.save()`, and `HazardDetailView` does `try? modelContext.save()` on the corrective-action status change (same `@Environment(\.modelContext)` pattern as `ChecklistItemRow`). File deleted (auto-removed from the build via the synchronized file group — 0 `pbxproj` references). **BUILD SUCCEEDED** (log confirms stale `HazardViewModel.o` removed); `rg` now finds no source references. Remaining ViewModels (Home / Inspection / Settings / StartInspection) are real and unaffected.
- [x] **SwiftData migration plan — documented (2026-06-18)** → `SWIFTDATA_MIGRATION.md` (new top-level doc, linked from CLAUDE.md Key Files). Documentation only; **no model/app code changed** (`xcodebuild build` SUCCEEDED). Captures: (1) the frozen baseline — 5 `@Model` types (Site/Area/Inspection/ChecklistItem/Hazard) with exact fields/optionals/enums, the 3 `.cascade` relationships (Site→areas, Inspection→items, Inspection→hazards) and the deliberate denormalized-id pattern (siteName/areaName + stored UUIDs) so deleting a Site never wipes inspection history; (2) current storage policy — default persistent container, **no `VersionedSchema`/`SchemaMigrationPlan` yet** (implicit lightweight inference), reset scope, and the UserDefaults↔SwiftData boundary (onboarding/appearance/inspectorName/RegionProfile are **not** reset); (3) change classification — ✅ lightweight-safe (add optional / add-with-default / new model / new enum case) vs ⚠️ needs an explicit migration (non-optional add w/o default, rename, delete, type change, relationship/deleteRule change, `@Attribute(.unique)`, photoPath-scheme change) vs ⛔ prohibited (changing an enum `rawValue` string — breaks decode of every persisted row); (4) EvidencePhotos↔row consistency rules (relative `EvidencePhotos/<uuid>.jpg`, delete row ⇒ delete file, `Hazard.photoPath` non-optional `""`=none); (5) standalone-Hazard (`inspectionId == nil`, not cascade-deleted) + cascade cautions; (6) a per-PR "SwiftData schema impact" checklist; and the trigger to introduce `VersionedSchema`(SchemaV1 baseline)+`SchemaMigrationPlan` on the first post-release model change. Verified against source via `rg` (`@Model`/`@Relationship`/`deleteRule`/`modelContainer`; confirmed no migration infra and no `@Attribute(.unique)` exist today).
- [x] **Accessibility — Dynamic Type / VoiceOver smoke QA passed (2026-06-18)**
  - **Dynamic Type** runtime-verified at **AX5** (`accessibility-extra-extra-extra-large`, via `simctl ui content_size`) on Home / Hazards / Settings: Home rail wraps "미처리 위험요인" to two lines with the count still visible and the 높음·보통·낮음 segments readable (no overlap); Hazard rows grow and the description wraps then truncates (acceptable for a list row) while the **filter bar is already a horizontal `ScrollView`** so the 시정 조치 상태 chip is reachable (scroll), not clipped; Settings Form rows grow, the "초기 설정 다시 보기" button wraps, everything scrolls. The app **does not clamp** Dynamic Type (`rg` finds no `.dynamicTypeSize`/environment override) and uses **semantic fonts** for text — the only fixed `.font(.system(size:))` are decorative (a 9pt risk dot, 52/44pt empty-state icons), not text. No catastrophic clipping/overlap.
  - **VoiceOver / labels** (code-reviewed): risk/status/result badges always render their **text label** (높음·보통·낮음 / 미착수·진행중·완료 / 적합·부적합·해당없음) so meaning isn't color-only; the Home rail uses `accessibilityElement(.ignore)` + combined `"\(label) \(count)"` labels; decorative icons are `accessibilityHidden`; risk/result chips carry the `.isSelected` trait. **One gap fixed**: the Hazards clear-filter `xmark.circle.fill` button had no label (VoiceOver read "x mark circle fill") → added `.accessibilityLabel` via a new localized key `hazard.clearFilters` (en "Clear filters" / ko "필터 지우기"; parity 280=280, plutil OK).
  - **Build/verify**: **BUILD SUCCEEDED**; seed data + content-size override cleaned up afterward (store empty, prefs unset). InspectionDetail + ChecklistView were **code-reviewed** (semantic fonts, `fixedSize(vertical)` wrapping, list-row `lineLimit`) but not AX-screenshotted — their entry points (NavigationLink card / in-inspection flow) aren't synthetic-clickable (safetywalk-qa-guardrails §10).
  - **Minor residuals (non-blocking)**: segmented controls (지역 / 화면 모드) have iOS-standard limited Dynamic Type scaling (text stays smallish but readable); at AX5, list-row descriptions and the filter chips rely on truncation/horizontal-scroll rather than full reflow — acceptable for a field tool, candidates for a later polish pass.
- [x] **`/design-visual-qa` on remaining screens — passed, no code changes needed (2026-06-18)**
  - Ran render → screenshot → rubric (Hierarchy / Spacing / Typography / Color&contrast / Alignment / Empty-state / field-tool intent) on a richly-seeded sim (2 inspections [completed + in-progress], 4 items incl. a long note, 3 hazards low/med/high with varied corrective status). **Fresh dark-mode captures of all 5 tab roots — all PASS**:
    - **Home**: clear focal hierarchy (status rail → 오늘/진행중 → 점검 시작 CTA → 최근 점검 cards); rail wraps gracefully; risk dots gold/orange/red; recent-inspection cards show 진행중(amber)/완료(green) badges + pass/fail counts. No overlap.
    - **Inspection (점검)**: active-inspection row + bottom-anchored 점검 시작 CTA; sparse with one item but correct (list grows; empty state exists). No breakage.
    - **Hazards**: 낮음=gold / 보통=orange / 높음=red badges, all with text labels; long description wraps to 2 lines; consistent photo placeholders; filter chips in a horizontal scroll. Clean.
    - **History (기록)**: site name prominent, 진행중/완료 status badges, dual date icons (start/complete), "3 / 3 항목 1 부적합" with the fail count in red. Good hierarchy.
    - **Settings**: sectioned Form, orange/red action styling (초기 설정 다시 보기 / 로컬 데이터 초기화), 현장 관리 NavigationLink, disclaimer card. Clean.
  - **No visual defects found → no code polish made.** The remaining screens were already iteratively refined by prior P0/P1/P2 work (Home redesign, PDF report, risk-color unify, contrast pass, a11y), so this pass is a confirmation, not a fix cycle. `xcodebuild build` **SUCCEEDED**; no console crash.
  - **Light/dark**: app light *and* dark rendering were already verified PASS in prior tasks (Delete/Reset: Home light + Home/Hazards/Settings dark; contrast: Hazards light+dark). This session's **fresh light captures were blocked by a stubborn simulator cfprefsd caching bug** — the app's container plist `appearanceMode` was set to `light` (PlistBuddy-confirmed) and SpringBoard was light, yet the app's prefs client kept serving the cached `dark` across reboot + `killall cfprefsd`. This is an **environment limitation, not an app defect** (screens use adaptive semantic colors — `Color(.systemBackground)`/`.primary`/`.secondary` + status tints — that already render correctly in the dark set). Detail/sub screens (Checklist / InspectionDetail / InspectionSummary / HazardDetail / HazardRegistration) are NavigationLink/toolbar-gated from synthetic clicks (§10); the inspection flow was previously marked PASS and the others were reviewed in prior tasks. Seed + test prefs cleaned up afterward (store empty, prefs cleared).
- [ ] In-app language toggle (3-3) is intentionally deferred to P2/post-MVP (documented in TASKS.md) — no action for MVP.

---

## P3 — App Store submission (non-code)

**Submission package drafted in `APP_STORE_SUBMISSION.md` (2026-06-18).** Metadata,
privacy answers, screenshots, reviewer notes, and the final checklist are prepared;
the items below track what is **drafted** vs the **human-only** App Store Connect /
Xcode steps that remain.

- [x] **App Store Connect metadata — drafted** (`APP_STORE_SUBMISSION.md §1`): KO+EN name / subtitle / promotional text / description / keywords; Category = **Business** (secondary Productivity); Age rating **4+** with questionnaire basis; copyright placeholder. **Human still:** paste into ASC, and provide **Support URL** + **Marketing URL** (required/optional fields — not yet hosted).
- [x] **Privacy label — drafted** (`§2`): **No data collected, No tracking** (offline-first, no networking/analytics/ad-SDK — rg-verified); only required-reason API is UserDefaults `CA92.1` (matches `PrivacyInfo.xcprivacy`); user-initiated Share-Sheet exception + on-device-only storage spelled out; PhotosPicker (out-of-process) vs sandbox `EvidencePhotos` distinguished. Includes a **privacy-policy draft** — **human still:** host it + a support page and paste the URLs (both required by ASC).
- [~] **Screenshots — candidates captured** in `docs/app-store-screenshots/` (5 PNGs, light mode, natural KO data: Home rail / Inspections / Hazards / History / Settings) at **1290×2796 (6.7")**. **Human still:** App Store Connect may require **6.9" (1320×2868)** / **6.5" (1242×2688)** — re-capture on those sims if rejected; and add the recommended manual-run shots (Checklist, Hazard detail, shared PDF report) that are navigation/share-gated from automation (§10).
- [x] **"No legal judgment" compliance — confirmed**: no screen frames Pass/Fail as an OSHA/KOSHA legal violation; risk levels are **reported tallies, never a verdict**; the **disclaimer** ("법적 위반 여부를 판단하거나 법적 준수 여부를 확정하지 않습니다") appears in Settings and on the exported PDF report. Store description + reviewer notes restate this. No legal-violation wording anywhere.
- [x] **QA test data cleared** — screenshots used a fresh seeded store; seed + test prefs cleaned up afterward (store empty, prefs unset). App icon / launch screen / PrivacyInfo / no-network all re-confirmed in the package's final checklist; `xcodebuild build` SUCCEEDED. **Human still:** Archive (Distribution signing — Development Team already configured) → validate → upload → (optional) TestFlight → submit.
- [x] **Localization QA (EN / KO) + "Global" label policy — verified, no code change (2026-06-18)**
  - **Resources/bundle**: `en.lproj` + `ko.lproj` bundled; `developmentRegion = en`, `knownRegions = (en, Base, ko)`. **String consistency**: en/ko **full key-set parity (280=280)**, `plutil -lint` OK both, **all 182 `LocalizationKey` cases resolve in both** (no raw-key leak), and the 6 template **category** keys + the global/korea **item** keys (data-driven `titleKey`s) all resolve in both. The extra ~98 strings keys over the 182 enum cases are the JSON-driven template item/category keys (expected).
  - **English runtime** (`-AppleLanguages (en)`): Home (empty + populated), Hazards, Settings render entirely in English — tab bar, CTAs, empty states, **risk High/Medium/Low**, **status Not Started/In Progress/Completed**, region picker, and the disclaimer. **Korean runtime** (`-AppleLanguages (ko)`): same screens entirely in Korean (also confirmed by the P3 KO screenshots). No raw keys, no stray-language UI chrome.
  - **"Global" / "글로벌" label decision → KEEP (no change).** `RegionProfile {korea, global}` is a **content/region profile** (Korea = `checklist_korea.json` KOSHA-oriented; Global = `checklist_global.json` "General Site Inspection"), persisted as `KR`/`GLOBAL` codes (independent of the Swift `rawValue` and of the UI language). The **global template's items are fully localized** (e.g. "Guardrails installed at all open edges above 1.8 m" ↔ "1.8m 이상 개방부 안전난간 설치 여부"), so it is **not** an English-only checklist — renaming the label to "English"/"영문" would be **misleading**. Display strings `settings.region.korea/global` (한국/글로벌 ↔ Korea/Global) are correct and consistent across Onboarding, Settings, and Template selection. No `RegionProfile` rawValue / stored-code change.
  - **App Store metadata**: English support is **real and runtime-verified**, so listing Korean + English is accurate (recorded in `APP_STORE_SUBMISSION.md §1`). **Minor (owner decision, not changed):** the on-device icon name is the brand "SafetyWalk" in both locales (not localized to "현장안전 지킴이") — fine to keep; the App Store *listing* name is localizable per store separately.
  - `xcodebuild build` SUCCEEDED; **no source/string/model change made** (localization already correct). Seed + language test state cleaned up.
- [x] **Global Readiness QA — English golden path + English/Global PDF (2026-06-21)** — `xcodebuild` **SUCCEEDED, 0 errors/0 warnings**; en/ko parity 280=280, plutil OK, key sets identical.
  - **English/Global PDF (the key deliverable) — PASS.** Generated the real report via `InspectionExportService.exportPDF` on a completed inspection whose checklist items use the **`checklist_global.json` `titleKey`s** + High/Medium/Low hazards, run under `-AppleLanguages (en)` (a temporary DEBUG-only export harness, since removed — net code change zero, `SafetyWalkApp` reverted, rebuild confirms `ReportExportQAHarness.o` gone). `pdfinfo`: A4-width 595pt, 1 page, Quartz. **`pdftotext` checks: 0 Hangul characters, 0 raw `checklist.item`/`checklist.category`/`report.` keys.** Rendered PNG visually confirmed everything in English — masthead "Field Safety Inspection Report", Inspection Information, Result Summary (Pass/Fail/N-A/Unchecked/Completion), Hazard Summary, **Checklist Results by Category** (Electrical / Fall Risk / PPE with the global items in natural English + Pass/Fail/N-A/Unchecked badges + notes), Hazards & Corrective Actions table (Low=gold/Medium=orange/High=red, English labels + status), 3-col Evidence grid, and the **English disclaimer** ("does not determine legal violations or provide compliance certification"). No truncation, no overlap, no stray language. Locale-aware dates (6/21/26 1:40 PM).
  - **English UI runtime — PASS** (re-confirmed from the localization pass): Home / Hazards / Settings render fully in English. Onboarding's English strings all resolve (its region picker reuses the Settings `Korea/Global` labels, already English-verified); a fresh runtime onboarding screenshot was blocked by the known simulator **cfprefsd flag-stuck** bug (env limitation, not an app defect). In-app Checklist-marking / detail screens are NavigationLink/toolbar-gated from synthetic clicks (§10), but their localized content is proven by the PDF (which renders the same global items + results + hazards in English) and by static key resolution. Share-Sheet display is language-independent and already verified in P1 Share/Export.
  - **Global content review** — no Korea-specific agency / law / address / phone / date-format leakage in the global path (rg-clean in EN strings + Views; report uses locale-aware `DateFormatter` + `NSLocalizedString`). Disclaimer (EN + KO) makes **no** compliance/violation claim. **Two P2 content-polish items (not blockers, not changed):** (1) `checklist.item.global.fall.001` states a single metric threshold "above 1.8 m" — fine globally (metric; ≈6 ft) and framed as an inspection *prompt* covered by the disclaimer, but a stricter-global phrasing could qualify it (e.g. "at open edges / elevated work areas per applicable limits"); (2) `checklist.item.global.elec.003` uses "GFCI" (North-American term; "RCD" is the IEC/international term) — widely understood, optional to generalize to "ground-fault / RCD". Per the "fix only P0/P1" rule these were left unchanged and recorded for the owner.
  - **Findings: 0 × P0, 0 × P1, 2 × P2** (above). No global-launch blocker. App display name "SafetyWalk" recorded as a branding decision (unchanged). Test data / language overrides cleaned up.

- [x] **Dark Mode launch experience — static Launch Screen made appearance-adaptive (2026-06-23)**
  - **Cause (diagnosed, static launch vs. SwiftUI separated):** The static iOS launch screen was the auto-generated *blank* one — built `Info.plist` had `UILaunchScreen = <dict/>` (empty, **no `UIColorName`**) via `INFOPLIST_KEY_UILaunchScreen_Generation = YES`. With no explicit background it relies on the system default, which is **not guaranteed dark-adaptive across the 17.0+ deployment range** (a real white-flash risk on older iOS even though iOS 26.5 happens to render it black). The **SwiftUI side was already correct**: `preferredColorScheme(appearanceMode.colorScheme)` at the `WindowGroup` root + `Color(.systemBackground)` backdrop (ContentView) and `Color(.systemGroupedBackground)` (Onboarding) are all adaptive; the white shield card in the intro is an intentional **app-icon-style brand mark** (matches `AppIcon`, stays light in both modes — not a bug). No hard-coded `.white`/`.black` *backgrounds* exist on the launch path (the only `.white` is inside the brand mark's nodes).
  - **Fix (minimal, no storyboard):** added an adaptive asset color **`LaunchBackground`** (Any = white, Dark = black — matches `systemBackground`) and connected it to the launch screen via a small partial **`Info.plist`** (`UILaunchScreen → UIColorName = LaunchBackground`). Build wiring (app target Debug+Release only): `INFOPLIST_FILE = Info.plist` (placed at SRCROOT, **outside** the Xcode-16 synchronized source group so it isn't double-added to Copy Bundle Resources), `INFOPLIST_KEY_UILaunchScreen_Generation = NO` (so the generated empty dict no longer overrides), `GENERATE_INFOPLIST_FILE` stays `YES` so all other keys still merge. **No change** to the appearance picker, onboarding, app icon, models, localization, or the intro design/timing/brand. Generated `Info.plist` verified: `UILaunchScreen→UIColorName=LaunchBackground` present and CFBundleDisplayName / MinimumOSVersion 17.0 / SceneManifest / PhotoLibrary usage / orientations all preserved; `Assets.car` carries `LaunchBackground` with **two** variants (light+dark).
  - **Runtime cold-launch verification** (real frames; app ad-hoc signed and installed; recordVideo @60fps + 5s stable shots; iPhone 17 Pro / iOS 26.5). Static launch follows **system** appearance (iOS cannot read the app's stored override before launch — documented constraint); app content follows the **app** appearance from a stable state:

    | # | System | App mode | Static Launch Screen | App content (stable) |
    |---|---|---|---|---|
    | 1 | Dark | System | **Black** (avg 16) — no white flash | Dark ✓ |
    | 2 | Light | Dark | White (follows system) | **Dark** ✓ (override applied — Home renders fully dark on a light system) |
    | 3 | Dark | Light | **Black** (avg 2) — no white flash | **Light** ✓ (override applied — Home renders fully light on a dark system) |
    | 4 | Light | System | White (follows system) | Light ✓ |

    In the matched cases (1, 4) static↔first-frame agree → seamless; in the forced-opposite cases (2, 3) the static screen shows the **system** appearance for one step before SwiftUI applies the app override — the unavoidable iOS platform constraint, now a controlled neutral `systemBackground`-matching surface rather than an undefined default.
  - **Build:** Release `xcodebuild clean build` **SUCCEEDED, 0 errors / 0 warnings** (built to a clean derived-data path; building into the Desktop project tree fails *CodeSign* with "resource fork…detritus" because macOS auto-stamps `com.apple.provenance` xattrs there — a pre-existing environment quirk, unrelated to this change and not hit by a normal Xcode build). Files changed: **`Info.plist`** (new, SRCROOT), **`Assets.xcassets/LaunchBackground.colorset/`** (new), **`project.pbxproj`** (INFOPLIST_FILE + UILaunchScreen_Generation=NO, app target ×2 configs). Sim test data + temp build dirs cleaned up.
  - **Residual platform constraint (unavoidable):** the static launch screen can only track the **system** appearance; when a user forces the app to the opposite of the system, the very first (static) frame matches the system, then the app corrects to its own setting on the first SwiftUI frame. No code can read the app's stored preference before the process starts.

- [x] **Monetization decision — v1.0 무료 출시 + Pro fast-follow (owner 확정 2026-06-21)** 🧭 — 비코드 제품 결정. **v1.0은 완전 무료로 출시하며 수익화는 출시 차단 요소가 아니다**(IAP/paywall/StoreKit/상품 미생성, Swift·프로젝트 설정 무변경, 커스텀 템플릿 미구현). 향후 Pro는 **non-consumable 일회성 IAP**(핵심 가치 = 커스텀 점검 템플릿, 보조 = 브랜드/고급 PDF)로 **fast-follow**; **기존 점검·기록 열람·삭제·기본 PDF·핵심 안전 기능은 무료 유지**, 가격 기준안 **₩24,000 / $19.99**. **광고·분석 SDK 미도입**(프라이버시 라벨 "데이터 수집 없음" 보존). **Team 구독은 계정·동기화·역할·다중 현장 구현 이후에만 재검토.** 근거·경쟁사 비교·매출 시나리오·Free/Pro 경계는 `MONETIZATION_STRATEGY.md`(§0·§11 owner 확정), Pro 착수 게이트는 `POST_LAUNCH_MONETIZATION_VALIDATION.md` 참조. **출시 영향 없음** — v1.0은 현재 무료·기능 완비 상태 그대로 제출 가능.

---

## Done = ready to submit

P0 all `[x]` + P1 all `[x]` + P3 metadata/privacy-label prepared. P2 is strongly
recommended but a judgment call per item.
