# 아이콘 이미지/ — 앱 아이콘 **후보 원본** (빌드 미사용, 보존용)

**이 폴더의 이미지는 빌드에 쓰이지 않는다.** 실제 앱 아이콘은 아래 정식 위치(`Assets.xcassets`)에
별도로 들어가 있다(오너 확인, 2026-07-20). 여기 4장은 그 이전에 만든 시안·원본이며 **참고용으로 보존**한다.

- 코드·`.pbxproj`·`Contents.json` 어디에서도 이 폴더를 참조하지 않는다(검증 2026-07-20, 참조 0건).
- 삭제하지 않고 남겨둔다. "어느 게 최종본이냐"는 질문이 반복돼 이 메모를 둔다.

## 실제 사용 중인 아이콘 (정식 위치)

| 타깃 | 경로 |
| --- | --- |
| iOS | `현장 안전 지킴이/Assets.xcassets/AppIcon.appiconset/AppIcon-1024.png` (+ `-dark`, `-tinted`) |
| macOS | `SafetyWalkMac/Assets.xcassets/AppIcon.appiconset/mac-16.png` ~ `mac-512.png` |

## 이 폴더의 4장

| 파일 | git 상태 | 시안 내용 |
| --- | --- | --- |
| `image.png` | 추적됨 (`5fcd239`) | 방패 + 4분할 + 경로선·주황 마커 |
| `image2.png` | 추적됨 (`5fcd239`) | 위와 유사하나 경로선·마커 없음 |
| `image3.png` | 추적됨 (`5fcd239`) | 방패만, 흰 배경 |
| `이미지.png` | **미추적**(로컬에만 존재) | 유리 질감 방패 + 주황 체크, 파란 배경 |

## 하지 말 것

- 이 파일들을 `Assets.xcassets`로 그대로 복사하지 말 것 — 최종 아이콘은 이미 정식 위치에 확정본이 있다.
- `V2_HANDOFF.md` WO-0의 `아이콘 이미지/` 언급 4곳은 **2026-06-26 구조 재편 기록**(부모 폴더 → repo 루트 편입)이다.
  현재 상태 설명이 아니므로 그 완료 기록을 고치지 말 것. 현재 상태는 이 파일이 담당한다.
